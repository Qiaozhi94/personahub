# PersonaHub V3.44 · 记忆归位到「内容 / 策略 / 诊断 / 效用」

V3 承接 V2 的对象模型与浅色视觉语言。V3.43 已把健康度重切为待办与读数；**V3.44 最终按职责归位：内容去记忆，策略去设置，故障去系统诊断，价值去统计**。此前版本的完整演进保留在下方历史修版记录与 `docs/design.md`。

> **V3.44 验收**：`browser-check.mjs` **111 条断言全过 · 0 控制台错误**，覆盖记忆待办、唯一配置入口、系统诊断、记忆效用、能力门禁，以及正式文案与数据表完整性；完整结果见 `browser-check.json`。
>
> **实现约束见 [`docs/implementation-notes.md`](./docs/implementation-notes.md)**——界面上撤下来的硬规则按面归档在那里，实现前逐条核对。

> 起点是 V2 最终态蓝图（`personahub-draft/personahub-v2.1/`）。

## V3.44 修版

| 用户要做什么 | 唯一落点 | 不再出现在哪里 |
|---|---|---|
| 处理记忆内容 | 记忆 · 待办 / 知识库 | 设置、健康页 |
| 调整记忆规则 | 设置 · 工作区 · 记忆 | 记忆 tab、系统诊断 |
| 排查应用故障 | 设置 · 应用 · 系统诊断 | 运行时、关于、记忆 |
| 判断记忆价值 | 统计 · 记忆效用 | 策略、健康页 |

「待确认」和「待处理」不再平级：它们合并为**待办**，内部按新候选、需要复核、整理建议分组，同一条只出现一次。系统诊断覆盖服务、数据库、事件流、记忆写入、索引关系、召回降级与磁盘快照，每行都有证据和修复动作。

配置去重后，自动入库、召回上限、上下文预算与整理周期只有**设置 · 记忆**一处可编辑入口。不可放宽的来源与可信边界改为只读说明。语义 / 混合召回仍按运行能力启用；最终设计稿完整呈现知识图谱，支持一跳与两跳关系、类型筛选、节点检索、节点详情和关系明细，具体发布批次不再限制页面完整度。

Skills 详情改为整页下钻：进入后隐藏 Skills / MCP 顶层切换，只通过面包屑返回能力或 Skills 列表。正文收敛为名称与说明、五项关键信息和只读文件区；步骤、完成标准与源文件重复的信息不再另行展示，所有示例 Skill 均可打开完整详情。

项目详情同步统一对象与版式：「工作流」改为 **Skills**，直接引用能力面中的 Skill，不再复制步骤与完成要求；默认 Skill 只在该页设置，项目设置不再保留重复入口。「知识」改名为**项目记忆**，明确它是记忆 · 知识库按当前项目作用域生成的视图；表格复用知识库字段，支持关键词、可信等级、记忆类型与生命周期组合筛选，并可定位到知识库中的同一条记忆。「项目记忆」与「Skills」均使用全站数据列表。项目设置固定为项目信息、代码仓、文件访问和项目管理：代码仓使用一个主目录与若干只读参考仓库的列表，网络和 `git push` 不在项目维度配置。知识图谱的关系明细也改用标准分区标题、次级操作按钮和统一状态标签。

「关于」进一步收成版本、更新与许可；数据目录、配置文件、快照目录、日志和诊断包导出只保留在**系统诊断**，避免两个设置页同时承担排障入口。

运行时删除独立「机器健康」区块：机器列表的点灯给整体状态，adapter 列表解释具体原因。系统诊断只增加一行执行机器汇总并跳转运行时，不复制逐机探针。

所有界面说明、操作反馈和数据字段统一采用面向用户的正式口径。39 组数据表均具备完整表头、可访问名称和明确的缺省状态，不使用空表头或破折号占位；这两项要求已纳入自动验收，后续实现必须保持一致。

## V3.43 修版

### 一、「健康度」为什么抓不住——它装了三张答不同问题的表

| 原来那一页的三张表 | 回答什么 | 看完要做什么 |
|---|---|---|
| 记忆债务 | 哪些条目坏了 | **去修**（每行都有操作按钮） |
| 写入侧健康 | 新东西进得来吗 | 排查管道 |
| 效用观测 | 这些东西有没有用 | 判断值不值得维护 |

第一张是**待办清单**，后两张是**读数**。而「健康度」是个**状态词**——它承诺「好还是不好」，打开却是三张表。

> **判据：一个 tab 的名字要和它里面每一行的动作对得上。**

改法：

- **「健康度」→「待处理」**，只留记忆债务。tab 上的 `11` 从一个含义不明的健康分变成「有 11 件事等你处理」。断言逐行校验：**给不出动作的行不该在这里**。
- **写入通路 · 效用 · 索引与召回** 三张读数移到策略，**跟着它们能调的旋钮走**。

### 二、策略成了闭环

```
写入 · 类型与强度 · 召回 · 保留     ← 旋钮
        试一次召回                 ← 验证
索引与召回 · 写入通路 · 效用        ← 读数
```

**「试一次召回」**是这一版最实的一块：此前策略让你改「每次最多 12 条」「预算 8000 token」，**改完没有任何地方能看效果**——和知识库缺「最近召回」是同一个毛病：给了旋钮不给读数。

现在输入一段任务描述就能看到：命中 4 条 · 过滤掉 2 条 · 占 1240 / 8000 token，**每一条写明为什么进来**，**被过滤掉的也列出来并写明为什么被挡**（`这一步是验证类，claimed 不进` / `超出「每次最多 12 条」之外`）。看不见被挡掉的，就判断不了阈值设得对不对。

### 三、知识库补两列 + 与图谱互通

| 列 | 为什么必须有 |
|---|---|
| **最近召回** | 策略里设了「180 天未召回提示退役」，界面却没这个数。超阈值的行自己标黄（`186 天前`），不用你拿 180 去逐行算 |
| **引用 · 采纳** | 引用是「被提到过几次」，**采纳是「改变过几次做法」**——决定要不要留一条看的是后者。采纳为 0 的压暗 |

**知识库 ↔ 知识图谱互通**：行内详情给「在知识图谱里看 →」，图谱给「回知识库看这一条 →」。两个面看的是同一批数据。

顺手修了两个 bug：墓碑那一行少一个格子（表头 9 列它 8 个）；策略里的表名「索引与运行时」与一级面「运行时」撞名。

## V3.42 修版

### 执行组合拆成两个下拉

照派工弹窗的口径：

```
模型      [ Sonnet 5 · Claude Code v1.9.0  ▼ ]
          只列当前可派工的；被额度或登录态挡住的不出现。
思考深度  [ low ▼ ]

          将派给  claude-sonnet5-low          ← 现算
```

**为什么拆**：ADR 0012 的四元组里，**深度是「每次派工可调，不烧进配置」的那一维**——它本来就该单独选。合成一个下拉时，想换个深度得在十几条组合里重新找同一个模型的另一条。`claude-sonnet5-low` 是展示用的组合名，不是输入方式。

**模型不支持的深度直接置灰**：`Kimi K2` 只有 low，选它时 medium / high 变灰，并把深度回落到 low。断言逐项验证：换深度预览要跟着变、换模型要置灰至少两档、深度要自动回落。

### 顺带把编辑态收回一屏

「重复触发」挪回触发条件那一卡，两列重新平衡到 4 / 3，去掉一条多余的 hint，Markdown 编辑框 12 行 → 10 行。

| | 加两个下拉之后 | 收紧后 |
|---|---|---|
| 编辑态高度 | 910（要滚） | **773（不滚）** |

## V3.41 修版

### 一、撤回三档产出

上一版加的「每次触发产出什么」整块移除，V3.20 的原判据恢复生效：**自动化建立的永远是普通任务**。

从提出到撤回只隔了一个版本。它解决的噪音问题是真的，但**三档摆在界面上的成本高过它省下的那点噪音**——真要做，正确的形态是让规则自己判断有没有发现，而不是让人先选一个模式。

### 二、属性页排版压紧

```
[ 触发条件        |  执行组合 ]     ← 并排，两块都短
  频率     每周·周一 09:00   执行组合  claude-sonnet5-low
  时区     Asia/Singapore    重复触发  合并进现有任务
  下次运行 明天 09:00        改动生效  从下一次触发起

任务内容（Markdown）                ← 全宽在下
```

三处改动：

| | 改前 | 改后 |
|---|---|---|
| 布局 | 四块竖排 | 前两块并排，字段在两列间平衡到 4 / 3 |
| 只读事实 | 标签一行、值一行 | **标签在左 76px、值在右，一行一条** |
| 间距 | 卡片 16px / 区块 26px | 12px / 20px |

> **判据：只读事实用两列，可编辑字段才用上下两行——前者是扫，后者是填。**

实测：只读态 773px、编辑态 773px，**都不需要滚动**（改前编辑态 858px 要滚）。

## V3.40 修版

### 推翻 V3.20：「仅运行」回来了，但分三档

V3.20 参考 multica 的 `run_only` 之后**明确裁掉了它**，判据是：后台静默跑会形成第二套不可验收的结果，绕开 Issue / Run / Artifact / Evidence 的来源链。

**那个判据没错，但它把一个真实需求一起否掉了**：一条每天跑的检查，大部分时候没有发现，每次都建一条任务就是噪音。

三档：

| 选项 | 说明 |
|---|---|
| 每次都建任务 | 不管有没有发现，都留一条可验收的任务 |
| **只在有发现时建任务** ← 默认 | 没发现就只留一条运行记录，不制造噪音 |
| 仅运行，不建任务 | ⚠ 结果只进运行记录，**不进验收链，也不写记忆** |

**中间那一档是这个需求的准确表达**：它不是「不建任务」，而是「**无事发生时不建任务**」——有结果的时候一定有任务，来源链不断。

第三档保留，但**把代价写在选项旁边**，不藏进文档。断言直接查这句话在不在。

### meta 行收窄

`PersonaHub · 近 30 天 16 / 18 已运行 · 最近一次 2 小时前 验证通过`

工作流、权限、标题模板都在别处说过，不重复。断言反向查：meta 里不得出现这三样。

## V3.39 修版

### 一、只读的收进标题，可改的留在主区

```
每周依赖与安全检查                          [开关] [编辑] [立即运行]
每周一早上检查依赖漏洞与过期版本，结论由另一个模型独立验证。
PersonaHub · 依赖与安全检查 · {{date}} · Coding 工作流 · 读整个代码目录 / 不可写 / 无 push
─────────────────────────────────────────
属性 | 运行记录

触发条件      每周 · 周一 09:00     下次运行 明天 09:00 · 还有 18 小时
执行组合      claude-sonnet5-low
任务内容      Markdown
```

**「概览」改名「属性」**——它装的是这条规则**是什么**，不是一份摘要。三块**都可编辑**，其余全部只读。

### 二、时间表不写 cron

查了 multica：它的时间表编辑就是 `weekly ▼ / 1 ▼ / 14 : 00 / Shanghai GMT+8`，下面一行 `下次运行：Mon, Aug 17, 2:00 PM · 2 天 15 小时`——**全程没有 cron**。照做：

| | 改前 | 改后 |
|---|---|---|
| 只读 | `每周一 09:00　0 9 * * 1` | 频率 `每周 · 周一 09:00`；下次运行 `明天 09:00 · 还有 18 小时` |
| 编辑 | 一个 cron 输入框 | `每周 ▼` `周一 ▼` 于 `09` : `00` `Asia/Singapore GMT+8` |

**协议名同理**：`HMAC-SHA256` / `Idempotency-Key` / `issues.opened` 是实现细节，使用者要判断的是「验不验签」「重复投递去不去重」「收不收编辑事件」。断言直接禁掉这三个词出现在触发条件里。

### 三、执行组合补成可编辑

只列**当前可派工**的组合——被额度或登录态挡住的不出现在选项里。

## V3.38 修版

### 一、自动化概览收成三层

```
标题
一句简要描述                              ← 从概览挪到标题下的灰字

[卡片行] 项目 · 标题 · 工作流 · 执行组合 · 权限   ← 生成的任务，只读
触发条件                                   ← 可编辑
任务内容（Markdown）                        ← 可编辑，排最后
```

**权限降为只读一张卡**：自动化的权限**跟项目走**，不在这里单独设——你说得对，自动化不需要再控一层。卡片上写清「跟项目走，在项目 · 设置里改」。

**可编辑的只剩触发条件与任务内容**，其余一律只读。断言直接查：编辑态里可编辑的块必须正好 2 个，卡片区不得出现 input / select。

**启用改成开关**：原来是一个 `已启用` 的 pill，看不出能不能点。现在是 switch，关掉时文字变「已暂停」并提示「正在跑的不受影响，到点不再触发」。

### 二、记忆：按 clowder 的分法拆开

查了 clowder 的记忆模块——它分**六个面**：知识动态 · 搜索 · 索引状态 · 健康度 · 图书馆 · 知识图谱。关键一条是**它把「索引状态」和「健康度」分开**：

| | 装什么 | 处理动作 |
|---|---|---|
| 索引状态 | 嵌入服务、向量表、段落向量、重建 | **重建索引** |
| 健康度 | 矛盾项、过期 / 未验证、孤立边、缺验证 | **逐条复核** |

PersonaHub 的健康度此前把两者混在一页。现在拆开：

- **健康度** 只留知识本身的三块：知识债务 · 写入侧是否正常 · 效用四层
- **索引同步 / 分词回退 / 关系重建 / 语义召回** 整块移到**策略** tab 的「索引与召回」

**「关系」改名「知识图谱」**（不自造词），页首改成「检索一条知识，看它由什么推出、被谁引用、和谁冲突」。

另外两条 clowder 的做法**暂不采纳**，理由写进 design.md §3.6.5b：独立的搜索面（当前记忆量个位数千条，知识库表头的搜索框够用）、图书馆 / 集合分层（会引出「一条记忆属于哪个集合」这个当前无人问的问题）。

## V3.37 修版

### 一、自动化概览：五块，任务内容排最后

「执行内容」与「生成的任务」并排时读不出差别。合成一块**任务内容**——使用者要写的其实就是**一份 Markdown 文本**：只读态渲染，编辑态给源码，可以直接改。

```
简要描述        一句话说清这条规则做什么          ← 新增，排第一
触发条件        什么时候开始
生成的任务      建出来的任务壳（项目/标题/标签/组合）
权限            能读什么、能不能写 / 出网 / push
任务内容        Markdown，每次触发原样发给 agent   ← 排最后，它最长
```

**任务内容排最后**：它是这一页最长的一块，排前面会把其余四块顶到屏幕外。新增断言直接查这个顺序。

### 二、记忆新增「策略」tab

原来散在待确认页里的「低风险自动入库」开关与「confirmed 只能由你来做」两段说明撤走，连同其余配置一起收进新的**策略** tab：

| 分区 | 装什么 |
|---|---|
| 写入 | 自动入库开关与条件；`confirmed` 永远需要人 |
| 类型与强度 | `type × stance` 白名单，一张可核对的表 |
| 召回 | 路数、每次最多带入几条、上下文预算上限 |
| 保留 | 多久未召回提示退役、遗忘、被替代 |
| 索引 | 当前状态与重建 |

### 三、三个面的页首

能力 / 记忆 / 统计原来是「一行 `small` 副标题 + 标题」，改成**标题在上、一句面向使用者的解释在下**：

> **记忆** — 从已验收的任务里沉淀下来、以后可以自动带进上下文的结论。只由验收事件写入，不从对话摘取。

## V3.36 修版

### 运行时不做任务控制

adapter tab 的「正在运行」去掉停止 / 结束按钮，改成只读，并写清去哪儿停。

> **判据：一个面的动作集合要和它的中心问题一致。**

运行时回答「我有哪些执行资源、每一样是什么状态」；中止一次执行回答的是「这一次活儿还要不要继续」——后者属于任务，不属于资源。

这也是一次回退：V3.33 已经把跨机器的「正在执行」整表移到统计 · 监控并保留停止入口，V3.34 却在 adapter tab 里把停止按钮又加了回来——**同一个动作有了两个入口，而其中一个所在的面并不为它负责**。

新增断言直接钉住：运行时的「正在运行」表不得出现 `停止 / 结束 / 中止 / 取消`，且必须指出停止该去哪里；同时正向验证监控那一侧确实有停止入口。

## V3.35 修版

### 工具不只有 MCP

原来那一块只写「已注入 4 个 server」，把 **CLI 自带的工具整个漏掉了**——而它们的状态恰恰会变：

| 工具 | 类型 | 来自 | 状态 | 说明 |
|---|---|---|---|---|
| 读文件 | 文件 | CLI 内置 | 可用 | 限项目授权的代码目录 |
| 写文件 | 文件 | CLI 内置 | **不先问你** | 写操作直接落盘，不出现「等待权限确认」 |
| 执行命令 | 命令 | CLI 内置 | 可用 | 在代码目录里执行 |
| 网络请求 | 网络 | CLI 内置 | 按项目边界 | 项目关掉网络时不可用 |
| `code-review-graph` | MCP | code-review 2.1 | **未注入** | 这个 adapter 不注入 MCP |

**类型**回答「这是哪一类能力」，**来自**回答「谁提供的」——两个问题分成两列，全部工具在同一张表里看。

这一问指出了一处真的遗漏：**「权限拦截」「原生记忆」这些能力位事实，本质上就是工具的状态**。「写文件之前会不会问你」写在「不支持」那一块，等于让人自己去和工具对应上。

**缺席逐条标**：OpenCode 那四个 MCP 工具各自标「未注入」，不是笼统一句。

### 宽度与卡片

- adapter tab 的内容**去掉 780px 限宽**，铺满右侧（实测 1138 / 1138）。
- 概览四张卡**一行铺开**；1400px 以下降为两列，900px 以下一列。

## V3.34 修版

### 一、「删除配置」删的是什么——这一版最实质的一条

使用者问：CLI 的配置不是在各个 CLI 里管的吗，这里的「删除配置」删的是什么？multica 没有配置这一说，clowder 才有。

**核对属实。** multica 的运行时页只有 `运行时 · 健康度 · 智能体 · 费用 · CLI` 五列，没有配置层——它的 CLI 全走各自登录；clowder 有，因为它确实存 API key 与 OAuth。

回到 ADR 0012，它把两层切开的**起因写得很具体**：

> OpenCode 一个安装可以同时挂国内 coding plan 与官方 API **两份凭据**；而 codex 与 claude-code **一个安装只有一种配法**。

所以两层是对的，**错在界面对三者一视同仁**：

| adapter | 这一层实际有什么 | 现在给什么 |
|---|---|---|
| Codex / Claude Code | 一份，内容全在 CLI 自己手里（登录态） | **只读事实 + 终端登录命令**，没有可增删的东西 |
| OpenCode | 真有两份，`base_url` 与 key 是 PersonaHub 存的 | 「接入方式」表，可新增 / 编辑 / 停用 / 删除 |

**规则**：只有一个 CLI 真有多套接入方式时，这一块才露出来。界面上「配置」正名为**接入方式**——一份接入 = 一套地址 + 一套凭据。

### 二、adapter 概览改卡片

四张卡取代原来那张十几行的定义列表：**状态**（在线 / 上次检查 / 版本）· **接入方式**（订阅还是按量，登录态从哪来）· **额度**（`5h 41% · 周 62%`）· **可用模型**（含并发）。

「不支持」只在**真有限制**时才出现——摆一块空的和「它没有限制」是两回事，前者要人自己去推断。

### 三、其余五条

| | 改前 | 改后 |
|---|---|---|
| 左栏标题 | 执行机器 | **远程** |
| adapter 状态 | 「可派工 / 额度偏紧 / 登录态过期」 | **只有在线 / 离线**；额度单独成列 |
| adapter 列表 | 无使用方式 | 加**使用方式**与**额度**两列 |
| 插件 | 有「同名冲突」区，让人选 | **重名直接拒绝导入**——冲突不该先被造出来再让人选 |
| 通知 | 三个渠道 | 去掉邮件 |
| 代码仓弹窗 | 要手填名称 | **从仓库名或目录名读出来** |
| 提交身份 | 单独一张卡，与 GitHub 连接重复 | **读执行机器上的 `git config`**，并进 GitHub 卡，不另存一份 |

## V3.33 修版

### 一、执行机器不再分本地与远程

从对象定义上统一：**服务端不直接执行任务，每一次派工都落到某一台注册进来的执行机器上**。机器一律用主机名（`qiaozhi-lt` / `mac-studio`），左栏不再分「本机 / 远程」两组。

**连带的一条硬判断**：应用自身的健康**不属于任何一台执行机器**。服务进程、SQLite、事件流、记忆检索归**服务端**，落到设置 · 关于；顶栏那个指示器指过去。过去它挂在「本机」上，只是因为那台机器碰巧同时是应用宿主——机型一统一，这个巧合就不能再当结构用。

每台机器只剩一致的**机器健康**：daemon / 网络 / 时钟 / 磁盘。

界面上「本机」这个词现在是 **0**。

### 二、概览加 adapter 列表

| adapter | 健康度 | CLI 版本 | 会话 | 可用组合 |
|---|---|---|---|---|
| ● Codex CLI | 可派工 | `codex 0.41.3` | ● 2 个会话 | 2 |
| ● Claude Code | 额度偏紧 | `claude 1.0.88` | ● 1 个会话 | 2 |
| ● OpenCode | 可派工 | `opencode 0.9.4` | ○ 空闲 | 2 |

点一行跳到它自己的 tab；上方是「＋ 添加 adapter」。**它和 tab 条不是重复**——tab 条给的是「切过去」，列表给的是「横着比」。tab 标签前也带在线灯。

### 三、执行组合表：去状态、加限额

| 组合 | 来自 | 使用方式 | 限额 |
|---|---|---|---|
| `codex-gpt5.6-high` | Codex CLI | 订阅 | 5h 41% · 周 62% |
| `opencode-api-kimi-k2` | OpenCode · official-api | 按量计费 | 不设上限 |

去掉状态列的理由：**健康度已经由上面那张 adapter 列表说完了，同一个事实不写两遍**。

### 四、adapter tab：概览 · 运行中 · 工具

配置卡去掉，重要信息一次列完：

**概览** — 状态 · 最近失败 · 版本 · 使用方式 · 认证 · 可用模型 · 时限额 · 周限额 · 并发 · 可用组合 · 可用项目 · 改动生效 · 不支持。分区标题右侧是配置的增删改（编辑 / 新增 / 删除配置 / 移除 adapter）。

**运行中** — 这个 adapter 上正在跑的**任务与会话**，可以单独停止或结束。

**工具** — MCP 注入了什么、缺席了什么。

## V3.32 修版

### 一、运行时：adapter 从表变成 tab

改之前是「一张 adapter 表 + 点一行从右侧弹 400px 抽屉」。三个问题：

1. **表里六列有四列（版本、配置数、可用组合、现在挡着什么）只是为了让人决定点哪一行**——那是导航，不是内容。
2. 抽屉一开，主区被压到 60%，而抽屉里装的正是刚才那一行的展开。
3. 换一个 adapter 要「关抽屉 → 找行 → 再点」。

改成 tab 之后，导航退成一行标签，内容占满整屏，换 adapter 一次点击，**状态灯直接挂在 tab 上**——不用点进去才知道哪个坏了。

```
左栏                右侧
本机                概览 │ ● Codex CLI │ ● Claude Code │ ● OpenCode
  本机
远程                概览：4 个读数 + 执行组合总览 + 健康探针
  mac-studio        adapter tab：状态 · 配置 · 工具 · 做不到什么
＋ 添加远程机器
```

**概览排第一**，装的是机器级的事：`adapter 3 · 可用组合 6 · 正在执行 2 · 最紧额度 41%`，然后是执行组合总览与健康探针。

**健康按机型分**：本机是「应用健康」（服务 / SQLite / 事件流 / 记忆检索 / 磁盘），远程机器是「机器健康」（daemon / 网络 / 时钟 / 磁盘）——**远程机器上没有应用宿主，不该出现 SQLite 这类探针**。

左栏按 `本机` / `远程` 分组，照 multica 的运行时页。

### 二、项目 · 设置改用设置面的表单形态

原来是左窄列标签的定义列表，和设置面两套样子。现在同一套：小标题 → 卡片 → 字段。顺带两处：

- 「能力边界」正名为 **权限**（同 V3.29 在自动化里做过的那次）。
- 补上**参考仓库**的选择——项目要读哪几个只读仓库，此前只能在设置里看，不能在项目里选。

### 三、代码仓合并成一个添加入口

不再让使用者先回答「本地还是远端」。填**本地路径或仓库地址**都可以，保存时按内容自动判断；是本地目录就读一次 git 配置，**识别到 remote 就把远端一起绑上**，识别不到就按纯本地处理并标明「拿不到 PR 与 issue」。

### 四、能力面 tab 栏那行说明去掉

## V3.31 修版

### 一、代码目录授权并进代码仓

原来它在运行时里按机器列一张表。但**授权跟着「哪个仓库」走，不跟着「哪台机器」走**——机器只是这个仓库的一个落点。搬进设置 · 代码仓的**配置弹窗**之后，一个仓库一屏，里面一台机器一行：

| 机器 | 路径 | 授权 |
|---|---|---|
| 本机 | `D:\Projects\personahub` | 读写 / 只读 / 不授权 |
| mac-studio | 未 clone | — |

**添加时可以直接选本地目录**，不必先去 GitHub 搜。选中后自动读一次 git 配置：识别到 remote 就把远端一并绑上（之后能读 PR 与 issue），识别不到就按纯本地目录处理并标明代价。

### 二、运行时机器页分两个 tab

一页里塞机器状态、执行组合、adapter 表、应用健康四块读不动。分成 `adapter` 与 `应用健康` 两个 tab，**adapter 永远排第一**——它是主体，诊断是补充。远程机器那一侧叫「机器健康」，探针换成 daemon / 网络 / 时钟 / 磁盘。

### 三、插件补配置弹窗

插件表列得出「1 个 MCP server、1 条定时规则」，却没有地方填 API Key 与抓取上限——**列得出参数却没有配置入口，等于承诺了一个不存在的东西**。配置项存在本机数据目录里，随插件一起卸载；弹窗同时列出它提供了什么、哪一项未生效。

### 四、通知重做

| | 改前 | 改后 |
|---|---|---|
| 应用内 | 不可关闭 | **可关闭**，全关时给一句警告 |
| 类型名 | 「验证未通过 / 连续两轮不收敛」 | `验证未通过` |
| 渠道列 | 事件表里每行重复一次 | **删掉**——渠道已由上面那张表管完 |
| 免打扰 | 无 | 时段内只记录不推送，结束合并成一条摘要 |

七个类型收成 `待授权 · 待指派 · 待验收 · 验证未通过 · 自动化结果 · 额度预警 · 运行时异常`。

### 五、偏好设置与目录清理

- **提交身份移到代码仓**——它是 git 的事，不是「我怎么看这台机器」。
- **界面语言去掉**：短期不做多语言，留一个只有一个选项的开关没有意义。
- 外观、时区、外部编辑器改成卡片表单，和个人资料同一形态。
- **设置左栏去掉每项副标题**，七行只剩七个词。
- **关于去掉「重新走一遍首次设置」**。

## V3.30 修版

### 一、划死那条线：读它的人是使用者，还是实现者？

上一版砍到 5.7k 字仍然太多，因为判断标准错了——我在问「这句话对不对」，该问的是「**读它的人是谁**」。凡是写给实现者的，一律撤离界面。

撤下来的不丢，按面归档进 **`docs/implementation-notes.md`**：

| 文档 | 记什么 |
|---|---|
| `design.md` | 为什么这样设计 |
| **`implementation-notes.md`** | **实现这一块时不能违反什么** |
| 界面 | 会发生什么、该怎么做 |

| | V3.27 | V3.28 | V3.29 | **V3.30** |
|---|---|---|---|---|
| 说明性文字 | 10,544 | 8,147 | 5,732 | **3,638** |
| 单段最长 | 447 | 201 | 196 | **133** |

按面看：运行时 808 → **290**，统计 573 → **189**，能力 661 → **166**，记忆 1620 → **1179**。

守门断言随之收紧：单段 **≤ 150 字**、整站 **≤ 4500 字**。

### 二、记忆面换成结果语言

`auto_inject: confirmed_only` 这种字段值直接摆在界面上，读的人得先知道 schema 才看得懂。

| 界面上原来写 | 现在写 |
|---|---|
| `auto_inject: confirmed_only` | 仅已确认可注入 |
| `auto_inject: only_as_candidate` | 仅作为候选 |
| `auto_inject: never` | 不可注入 |
| 记录 `superseded_by` | 记录替代来源 |
| 由 `content_sha256` 派生 | 由内容哈希派生 |
| 单路召回在跑（RRF 形状） | 单路召回在跑 |
| 被引用 2 条（ADR 0006 并行边界 · 任务 #133） | 被引用 2 条（任务 #133） |

记忆面里的 ADR 编号、章节号、字段名现在是 **0**。

### 三、能力面删掉「订阅」tab

它是**插件贡献的示例**，不是宿主自带的东西——摆在 Skills、MCP 旁边，看起来像三个同级能力。删掉之后能力面只有 **Skills 与 MCP**，其余由插件装出来。

页首那句「这一面装的是缺了也照样能干活的东西」收成两个字「能力」，说明落到 **tab 栏右侧**：`缺了也不影响任务正常执行的可选能力`，紧挨着「＋」。

### 四、偏好设置补个人资料

| 分组 | 字段 |
|---|---|
| **个人资料** | 头像 · 姓名 · 邮箱 · 关于你（随任务发给 agent） |
| **提交身份** | Git 用户名 · Git 邮箱 |

**两组必须分开**：显示名只影响界面，而 Git 身份**会写进 agent 产生的每一个 commit 并随仓库公开**。合成一张卡是从多人 SaaS 抄来的形状。

### 五、不预设部署形态

设计稿按**可交付、可指导开发的完全体**写，所以删掉了「单用户本机工具」「本机 · 不上传」「单用户本地模式」这类限定语——它们把一个产品形态写死在了界面文案里。头像菜单里那个禁用的「单用户本地模式」换成了个人资料与退出登录。

## V3.29 修版

这一轮全部来自使用者的逐条指出，改的是**命名、归属与文案密度**——没有新增功能。

### 一、运行时：机器改左栏列表，「运行中」搬走

tab 条的宽度由机器数量决定，接到第五台就开始挤；换成左栏列表之后形态与自动化、设置一致，也放得下「地址 / 在线 / adapter 数」这类必须跟着机器走的次行信息。跨机器的两个动作（额度告警阈值、暂停全部派工）落到左栏页脚。

**「运行中」整块移到统计 · 监控。** 两个口径不同：运行时回答「我有哪些执行资源」，是存量；正在跑几个进程是此刻的读数，和失败率、排队时长同类。搬过去之后跨机器合并成一张表，**带「执行机器」列**——不说清是哪一台，「去停掉它」就不知道该在哪台机器上执行。

### 二、取消「能力包」

它是本设计自己发明的第三个名词。声明式的一包 Skill 和带代码的一个插件，在使用者眼里都是「我装了个东西」，区别只在风险，而风险已经由「本机没有沙箱」和逐项确认的准入流程表达完了。

> **判据：一个名词如果只能靠它旁边那段解释才立得住，它就不该存在。**

装进来的一律叫**插件**。能力面只有 **Skills / MCP / 插件贡献的 tab**；治理集中在设置 · 插件管理，一张表列出每个插件**提供什么**（几条 Skill、几个 MCP server、哪个 tab、哪个 adapter）。

### 三、自动化：四层收成两层，四块改名，补编辑态

看一条规则时第一个问题是「它什么时候跑、跑出什么来」——触发条件放在另一个 tab 里，等于要求你记住上一屏再切回来。**触发器并进概览，Webhook 投递并进运行记录。**

| 现在叫 | 回答什么 | 原来叫 |
|---|---|---|
| **触发条件** | 什么时候开始 | 触发器（独立 tab） |
| **执行内容** | 每次触发时 agent 收到的指令 | 任务说明 |
| **生成的任务** | 建出来的那条任务长什么样 | 建成什么样的任务 |
| **权限** | 能读什么、能不能写 / 出网 / push | 能力边界 |

改名不是措辞问题：「任务说明」与「建成什么样的任务」并排时读不出差别；「能力边界」是自造词，它管的就是四项权限。

**每一块都有只读与可编辑两份**，就地切换、不另开弹窗——四块之间互相约束（换执行组合会影响权限的可选项），拆进弹窗就要在两个地方各看一半。此前 sp-bar 上那个「编辑」按钮承诺了一个不存在的东西。

### 四、设置表单照 multica 重做

「通用」与「关于」原来用的是左窄列标签的定义列表，那个形态适合**报事实**，不适合**改东西**——输入框无处可放，「改名」只能做成行内链接。改成 **小标题 → 卡片 → 字段（标签在上、控件在下）**，保存与取消在页头，一页一次提交。危险动作单独一段：只剩一个工作区时「删除工作区」**置灰并写明原因**，不隐藏。

**标签表铺满主舞台宽度**，列名正式化：`标签 · 说明 · 关联任务 · 最近使用 · 引用规则 · 操作`。

### 五、文案深度审视

| | V3.27 | V3.28 | V3.29 |
|---|---|---|---|
| 说明性文字 | 10,544 字 | 8,147 字 | **5,732 字** |
| 单条最长 | 447 字 | 201 字 | **196 字** |

分界线定死：**只留「会发生什么」与「该怎么做」**。删掉的是设计论证、参考项目对比、文档坐标（ADR 编号与章节号），以及写给设计者的备注——「写后果，不写能力位名字」「有 steps 才是编组；步骤只在详情里，不占列表一列」这类。

三条硬断言防回潮：说明段落不得超过 200 字、界面不得出现「能力包」、不得留写给设计者的备注。

## V3.28 修版

这一版动的是**归属**，不是页面细节：每一项设置该挂在哪个作用域下。

### 一、运行时重新成为一级工作面（§3.7）

**口径变了，所以位置才变。** 此前它的中心问题是「现在能不能派工；派不出去是因为什么」——那是排障口径，它把这一面定义成一个只在出事时才去的地方，V3.21 因此顺理成章把它并回了设置。

改成**盘点口径**：**「我对接了哪些 adapter、有哪些执行组合、还剩多少额度、带哪些工具」**。这样它和记忆（攒下的素材）、能力（可选的方法）落在同一根轴上，一级入口的位置随之定了——`记忆 · 能力 · 运行时 · 统计 · 设置`，**组内按依赖顺序排**（能力面自己写着「有哪些执行组合由运行时的 adapter 检查决定」，运行时是它的上游）。

编号恢复为 §3.7（V3.15 抽出时就是这个号，V3.21 并回设置时删掉），修订记录里 V3.15–V3.17 那几处引用因此重新指得到东西。

**排障没有被丢掉，它降为清单里的一列。** 判据：一张列出「支持 `codex-gpt5.6-high`」却派不出去的组合清单**是在撒谎**——「有哪些」和「现在能不能用」是同一行的两列。

三处新增：

| 新增 | 为什么 |
|---|---|
| **执行组合总览** | 盘点口径下第一个该被答上的问题此前答案是散的——要点开三个 adapter 各看一眼才拼得出来 |
| adapter 配置的**使用方式**（订阅 / 按量计费） | 此前从认证方式**推**：OAuth 算订阅、API Key 算按量。而 API Key 也可能挂在预付账户上，**推错就是把一笔钱记进另一本账**，且不会报错 |
| 本机 tab 的**应用健康**与**代码目录授权** | 本机同时是执行机器和应用宿主，所以它的 tab 比远程机器多两块。这个不对称是事实——远程机器上没有你的 SQLite |

使用方式那条有三个约束：**必选、无默认值**（给默认值等于替他决定这笔钱算不算支出）；**摆在配置卡最上面**（它一改，额度行、告警阈值、统计归属三块形态全变）；**不是认证方式的第二个名字**（`API Key + 订阅` 是合法组合，能表达它正是加这个字段的原因）。

### 二、设置从「三组九项」改为「两组七项」（§3.5）

> **判据：分组按作用域，不按主题。**

V3.23 的三组是按主题分的（工作习惯 / 执行环境 / 系统）。作用域回答一个主题答不了的问题：**我改这个会影响谁**。「偏好设置」和「标签」在主题上都算工作习惯，但前者只改变这台机器上的显示，后者改变整个工作区的分类。

| 组 | 项 |
|---|---|
| 应用 | 偏好设置 · 插件管理 · 通知 · 关于 |
| 工作区 | 通用 · 标签 · 代码仓 |

四处调整：

1. **任务前缀并进「通用」，标签独立成项。** 前缀是工作区的标识属性，和名称、描述同层；标签是一类对象的集合。两者原先并排，是因为碰巧都跟任务有关。
2. **新增「代码仓」**（下一节）。
3. **「能力包与插件」缩为「插件管理」**，能力包移交能力面。划的线不再是「声明式 vs 代码」，而是**风险治理 vs 能力选用**：能力包是你要**挑来用**的（能力面），插件是你要**盯着**的（本机没有沙箱，同权运行）。能力包的管理视图从 Skills 表的**来源列**点进去，**不重开「来源」tab**——V3.21 删它的判据「来源是治理维度，不是查找维度」仍然成立。
4. **删掉「数据与备份」。** 理由不是「参考项目没有」（它们的数据不在使用者机器上，那是部署形态不同）；是**页面承诺的严肃度和它能兑现的不匹配**——完整的备份/恢复页前提是恢复真的跑得通，而这一版给不出。**没有恢复入口的备份不成立，只有界面没有实现的恢复更糟。** 三行事实（数据目录 · 配置文件 · 服务端口）加「本机快照在哪」并进「关于」，**明文 API Key 的警告必须跟着数据目录一起留下**——它是全稿唯一说明密钥真实存放位置的地方。

### 三、代码仓：只读引用不能借用执行边界的壳

起因是一个真实场景：在 personahub 项目里派一个任务，让 agent **去分析 multica 仓库的代码**。此前没有落点——Project 通过 `default_workspace_id` 绑一个代码目录，那个目录是 personahub 自己的。

不能直接把 multica 配成第二个 Workspace，因为 Workspace 的定义带着执行语义（PRD：`agent run 的当前工作目录`、`workspace 写锁和并发控制`、`git repository / branch 状态`）。**只读地看一眼别人的代码，不该把写锁和分支状态一起拖进来。**

| | 主代码目录 | 参考仓库 |
|---|---|---|
| 数量 | 每个项目 1 个 | 0..N |
| 权限 | 可写 | **只读** |
| 带什么 | 写锁、git 分支、执行 cwd | 什么都不带 |

**它吸收了原来的「代码目录授权」，不建第二张表**，多机形态下拆成两层——`设置 · 工作区 · 代码仓` 定义「有哪些仓库」（跨机器），`运行时 · 机器 tab · 路径` 定义「它在这台机器上落在哪个路径、授权到读还是写」。这一刀是多机逼出来的：同一个仓库在三台机器上有三份 clone、三个路径，**而「有哪些仓库」只有一个答案**。

**标签同样定在工作区维度**：单用户本机标签总量小，每个项目一套会立刻长出同名不同义；而标签只是筛选层，不承担隔离职责——需要隔离的东西（Issue、Evidence）按项目隔离，那是 PRD 定死的。

### 四、统计的「失败」扩为「监控」（§3.4.1）

原判据是「统计全屏不出现一个实时数字」。这次划清了它拦的到底是什么：**它拦的是基础设施实时数字，不是执行层事件**。

| 进监控 | 不进监控 |
|---|---|
| 后台任务、失败率趋势、失败任务列表 | SQLite、事件流、磁盘、记忆检索（归运行时本机 tab） |
| | 额度（是资源余量，归运行时配置卡） |

两条硬要求：**刷新间隔与上次刷新时刻写在页面上**（近实时的前提是读者知道「近」到什么程度，否则周期口径的 KPI 卡和此刻口径的后台任务并排出现，没有任何东西说明哪个是几分钟前的）；**「运行中」不做两份**——运行时那份是**能停哪一个**（操作），监控这份只有计数与排队趋势（观察），点进去跳回运行时。

### 五、MCP 提为能力面的一个 tab（§3.2.2）

**V3.23 判过「MCP 不单开一页」，这一版推翻它。** 当时的理由是 MCP 只有一个来源（能力包声明），没有第三方 server 的用法。**推翻的是结论，不是判据**——「不为不存在的用法建管理页」仍然成立，只是这一版按最终形态画，而第三方 MCP server 在最终形态里几乎确定会有。

MCP 归能力面而不是运行时，用的是 §3.2.1 那张表切的那一刀：**缺一个 MCP server 少一样工具，缺一个 adapter 派不了工。**

**这张表有一列不能省：「在哪些 adapter 上已同步」。** MCP 的生效是 per-adapter 的（各 CLI 各自的配置文件），没有它就会出现「我装了，但 Codex 那份没同步」，而两次执行看起来一模一样。

**adapter 配置里的「工具」表不能删**：能力面那张是**意图**（我声明装了什么），adapter 那张是**结果**（它实际有什么）。两个都要，因为它们会不一致，而不一致是静默的。

### 六、文案审视：界面只留事实与动作

同一轮里把全稿的说明性文字按商业产品的标准过了一遍，参照 multica 与 clowder 的写法——**字段名 + 一句说清它对你有什么后果**，不解释设计为什么这么定。

删的是三类：**设计论证**（判据、为什么不这么做、两个参考项目怎么做）、**文档坐标**（ADR 编号、§ 章节号）、**元评论**（「这一页因此不再是……」「照 clowder 的做法」）。留的是两类：**会发生什么**（改前缀要重命名 412 条任务、停用后历史记录不变）和**该怎么做**（先去改规则、去终端跑这条命令）。

| | 改前 | 改后 |
|---|---|---|
| 说明性文字（pane-note / dl-sub 等） | 10,544 字 | 约 8,100 字 |
| 单条最长 | 447 字 | 201 字 |
| 「判据 / 债务展览馆 / 照 clowder / 打补丁 / 中心问题」 | 出现 | **0** |

新增一条断言直接查这件事：**界面文字里不得出现设计论证用词，说明段落里不得带 `（ADR …）`／`（§…）` 这类文档引用**。

> 这条不是新规矩，V3.26 已经对运行时右框写过一次「论证不进界面」——这一轮把它推到全稿。

### 七、两条推翻的记录

这一版推翻了两条本设计自己写下的结论，理由都是同一种：**当时的论证成立，但它证的不是使用者要的那件事**。

| 被推翻的 | 当时的论证 | 为什么证不到 |
|---|---|---|
| V3.21「运行时并回设置」 | 到达运行时的主路径是跳转，不是左栏点击 | 这句话现在仍然成立，但它证不出「不该有一级入口」——**记忆面的主路径同样是跳转** |
| §3.4「统计不出现实时数字」 | 同屏并列会出现两个时间口径 | 它拦的是**基础设施**实时数字；使用者要的是**执行层**事件，两者失败模式与处理动作都不同 |

新增 §7.8 记下一条待观察：**应用健康放在运行时是暂定的**，判据给死——「如果有人为了看『应用本身好不好』而必须先选中一台机器，就搬回设置」。

## V3.27 修版

按**增删改查**把设置里的每个对象核了一遍。判据：一个对象在界面上出现，它的增 / 查 / 改 / 删就都要有落点；某一格确实不该有，也要写明为什么。

**漏得最狠的是 adapter 配置。** 设计文档反复引用「删除一份配置会让依赖它的执行组合从派工清单消失，但历史 Run 保留当时的组合名」——而界面上**根本没有触发它的地方**；同样，「两者都不会把配置自动停用，**停用是你的决定**」这句话，承诺了一个不存在的动作。

补齐六处：

| 对象 | 补了什么 |
|---|---|
| adapter 配置 | 停用 / 启用、编辑、删除 |
| adapter | 从这台机器移除 |
| 机器 | 重新配对、移除这台机器 |
| 标签 | 重命名 / 合并到… / 删除（被规则引用时删不掉） |
| 能力包 · 插件 | 卸载（与「停用」分开） |
| 任务前缀 · 时区 · 外部编辑器 | 确认弹窗 / 可编辑入口 |

### 三条从 clowder 与 multica 借来的做法

1. **先给可逆的那一档。** multica 的智能体详情顶部常驻「已归档 · 无法被分配或提及」加一个「恢复」——归档是可逆的软删除。落到本产品：配置此前只有删除，而删除会连明文 key 一起带走；**连不上、key 过期这类，要的是先停掉别再派工，不是删**。
2. **引用计数常驻在对象上。** multica 的 skill 详情写着「被 0 个智能体使用」「还未分配给任何智能体」。所以配置卡上常驻「被 N 个执行组合引用 · N 条自动化规则用到」——**删除弹窗因此只是确认你已经看见的东西，不是第一次告诉你**。
3. **写明「你能对它做什么、改动何时生效」。** multica skill 详情有一个「权限」区：「你可以编辑和删除这个 skill。修改在智能体下次运行时生效。」本产品有正在跑的进程，这件事更不能不说：**改动从下一次派工起生效**，正在跑的不受影响（换配置要冷启动一个新进程，ADR 0009）。

clowder 那边确认了一个已有做法是对的：成员列表 Owner 那行直接写「**只能编辑，不能新增或删除**」——不能删的东西要在行上写明为什么，同本设计的「内置来源不可停用」。

### 删除弹窗：六处共用一个形态

三块，缺一不可：**这会动到什么**（现算的影响面）· **这不会动到什么**（历史 Run、证据链、验收结论一律不动）· **退路**（每日快照最近一次几点、从哪恢复、代价是什么）。

第二块**不是安慰，是必需项**——不确定删了会不会把历史一起带走时，真实反应是不删，于是界面上永远堆着一批不敢动的东西。

**不做「输入名称以确认」。** 两个参考项目都没有这道闸，而本产品每天有快照，真正的安全网是它。到处都要抄一遍名字，人会条件反射地照抄，那道闸就不再拦住任何东西。

**被引用时不给删。** 标签被自动化规则引用时，主按钮变成「先去改这些规则」而不是「删除」。

### 用词

全稿与原型里的「**弹层**」统一改成「**弹窗**」（45 处），它更常用。

## V3.26 修版

**adapter 表提到「运行中」上面。** 两条理由：**master 与 detail 的起点要对齐**——运行中排在前面时，adapter 表被顶到半屏以下，而点第三行弹出的右框却从顶部开始，视线要跳一次；**稳定的内容不该被易变的内容顶着走**——运行中的行数随进程增减，adapter 表是这一页的主体却会跟着上下漂。

**右框取消了框内的「配置 / 诊断」两个 tab。** 判据用的是 §3.2 里已经写过的那条：**tab 之间是并列的几组东西，不是一棵树**——而这一框装的是「同一个 adapter 的一串事实」，是线性的；何况诊断那一侧只有两行，撑不起一个 tab。改成四块固定顺序的分区：**状态 · 配置 · 工具 · 做不到什么**。

**属性一律收进它所属的那张配置卡。** 可用模型与深度、时限额与周限额、并发、项目可用性，全都是 `AdapterConfig` 的字段（ADR 0012 第 2 条）。V3.24 把它们摆在配置卡外面，结果 OpenCode 那两份只能靠 `coding-plan` / `official-api` 前缀硬拼在一起——**那是个补丁，和「删掉的五样」第一条批过的「模型表额度列只能靠『同池』打补丁」是同一类错误**。收进卡里之后归属由结构表达，连「额度挂在配置上」这句说明都不再需要。

**论证从界面撤回文档。** V3.24 的右框里塞了大段设计理由（为什么不折算次数、为什么额度挂在配置上、什么叫债务展览馆）。那些属于 `docs/design.md`。界面只留使用者要用的事实，**唯一的例外是解释一处缺席**——「登录态不代管，所以这里没有输入框」这类，因为使用者会在那儿找一个不存在的字段。

同一条标准也用在了左栏：adapter 表下面那段（三行 ADR 引用）收成两句怎么读这张表；运行中下面那段收成三句可操作的；远程机器那块留下后果与「远程执行在 v0.7」，删掉元评论。**验收里加了一条断言直接查这个**——整个机器 tab 内不得出现「判据 / 债务展览馆 / 打补丁 / 中心问题的另一半」这类词。

## V3.25 修版

> 一处改动，但牵动四个地方。

**额度不再折算成「还能派几次」。** 界面上原来写的是「剩 214 次 low ／ 42 次 high」，那是拿一个固定的深度换算系数（`high ≈ 5 次 low`）从剩余百分比反推出来的整数。使用者说不需要它：「我大概能评估出来，只需要给我时限额和周限额即可。」

**采纳的理由不是「用户要的」，而是它本来就错。** 真实消耗按任务差异极大——同样一次 high，读三个文件和跑一轮全量验证不是一个量级；**反推出的整数带着一种它没有的精确感**，而使用者自己对「还能干几件事」的估计反而更准，因为他知道下一件事有多大。这与 ADR 0017 自己的原则同源：那份决策引用 clowder 的原话要求订阅等价成本 *never present them as actual billing*，而**把折算值呈现成计数，和把折算成本呈现成账单，是同一类错误**。

改成每份 adapter 配置报两行上游自己给的口径：

| | |
| --- | --- |
| **时限额** | 滚动窗口（例如 5 小时）的已用百分比 + 重置时间 |
| **周限额** | 本周已用百分比 + 重置时间 |

三条约束：**取更紧的那一个**作判断；**重置时间必须给**——只有百分比时「等一会儿就好了」判断不出来，而它决定这一刻是该等还是该换一份配置；走 API Key 的配置两个都没有，写「不设上限 · 按 token 计费」，成本归统计面。

**跟着改的四处**：派工弹窗顶部那句额度说明、模型行上的额度标记、深度滑条下面的估算行（从「约够 7 次调用」改成报两个口径并指出哪个更紧）、额度不足挡下派工时给的理由（不再说「不足以完成一次 high（需 5 次）」，改成说窗口用了多少、几时重置）。告警阈值 20% / 10% 的口径也随之变成「两者中更紧的那一个的剩余量」。

**深度换算系数从界面上全部消失**，包括解释文字里——把它印出来只会让人拿它当真。

**动了一份 ADR。** ADR 0017 第 2 节那格写着「额度＝当前还可派出几次 high」，与这一改直接冲突。按 ADR 0006 立的规矩（「若调整幅度大到改变本决策的结论，通过补充或 superseding ADR 记录，而不是直接在 design.md 里悄悄偏离且不留痕迹」），在 ADR 0017 里就地加了一段 **2026-09-04 补记**：结论（额度属 Runtime、不进统计模块、三个口径永不相加）一条没动，改的只是额度在界面上长什么样。

## V3.24 修版

> 本段只覆盖设置 · 运行时。

**机器成了一层，因为它是 adapter 自己答不了的那一层。** `runtime_id` 早就是 `AdapterConfig` 与身份快照上的字段（ADR 0015 第 1、2 条，v0.3–v0.6 恒为 `local`，远程执行在 v0.7），界面现在按目标形态搭：**一台机器一个 tab，每接一台远程机器就多一个 tab**。

不能省这一层的理由是一个具体失败模式：**机器掉线时，它上面所有 adapter 都派不了工，而每个 adapter 自己的检查仍然显示「上次检查通过」**——那是上一次能连上时的结果。状态因此分两级报：先机器，再 adapter。multica 的运行时页同样按电脑分组（ADR 0015 的对比表已记下）；clowder 只在 API server 那台机器上 spawn，所以它没有这一层。

**tab 内三块，顺序即优先级**：这台机器（地址 / daemon / 在线 / 时钟偏移）→ 运行中（这台机器上正在跑什么，可以单独停）→ adapter 表（`状态 · adapter · 版本 · 配置 · 可用组合 · 现在挡着什么`）。**点 adapter 那一行弹右框**看详情，右框内仍是配置 / 诊断两个 tab。

**V3.17 的「主面永远是某一个 adapter 的详情」被替换了。** 那条防的是「同一块地方要维护『选全部』与『选一个』两套内容」；现在列表与详情是两块地方、各有各的职责，那个失败模式不成立——换上来的是 §3.2.3 那条「列表回答挑哪一条，详情回答这一条是什么」。「没有『全部』总览行」继续有效。

**两条跟着机器层走的调整**：「运行中」从「本机维度」归到 tab 内（它本来就是 per-机器 的事实）；**额度告警阈值再提一层到 `sp-bar`**——它跨机器生效，而要改一条全局策略不该先被迫选中某一个具体对象。这已经是它第三次搬家，每次都是同一条理由。

**逐项审视，删掉五样**：

| 删掉的 | 为什么 |
|---|---|
| 模型表的**额度**列 | 额度按配置分池，给每个模型开一列最后只能靠「同池」两个字打补丁——**一个需要补丁词才能自圆其说的列，本身就是错的** |
| 模型表的**状态**列 | 三行恒为「可用」；不可用的模型根本不会进「可用模型」清单 |
| 额度池表的**配置 / 认证**两列 | 单配置视图里恒等于上面那份配置已经说过的 |
| 工具表的**缺了会怎样**列 | 一整列只有一行有值——**只有一行有值的列应该是行内说明，不是一列** |
| 诊断的**近 30 天派工计数**与**登录态到期** | 前者是回顾，口径归统计面（运行时是前瞻，同屏并列会出现两个时间口径）；排障要的是「最近那一次失败是什么、点得进去」。后者在配置里已经写过一遍 |

**同时补回来一条**：「登录态不代管」不能随页首一起删——它回答的是**「为什么这里没有输入框」**，所以贴到 OAuth 那两份配置旁边写。

**执行位置不进执行组合名。** 组合仍是 `adapter × 配置 × 模型 × 深度`；位置进身份快照的 `runtime_id`。否则同一个组合在两台机器上会变成两个名字，现算表现也被切成两半。**同名 adapter 在两台机器上是两份东西**（两份安装、两份登录态、两个额度池），列表不做跨机器合并——合并之后「去重新登录」就不知道该在哪台机器上执行了。

**留了一个没答的问题**（§7.7）：接上第二台机器之后，「同一个模型跑在两台机器上算不算同源」会立刻冒出来。ADR 0015 明确把这个语义留到 v0.7，所以这一版界面**不给它任何暗示**——机器 tab 只表达「在哪执行」，不参与独立性判定的措辞。

## V3.23 修版

> 本段只覆盖设置面与它牵动的两处（能力面的工作流合并、派工弹窗的「本次生效组合」）。

**「乱」有一个可判定的成因：一个导航列表同时装了三种东西。** 改版前设置的一级目录八行里，既有**类别**（任务前缀与标签、数据与备份），又有**实体实例**（Codex CLI / Claude Code / OpenCode），还有**动作**（＋添加 adapter、首次设置向导）。后果有三条：列表长度不再由设计决定而由「装了几个 CLI」决定；点击契约不一致（多数行换一页，adapter 行换页并选中）；**风险最高的流程长得最便宜**——「添加 adapter」是 T2 代码准入，却和「任务前缀与标签」视觉重量一样。

**运行时收成一行，adapter 回到 pane 内左框。** §3.5.4「这一组有左框」那条论证一个字没改——那个左框本来就是 pane 内部的，V3.21 只是把它焊到了一级目录上。同时补上一条硬规则：**一级目录不列没有页的行**，改版前有三行是点了只弹一句话的死路，而 demo 死路比缺项更伤，它承诺了一个不存在的东西。

**补齐五页**：偏好设置（外观 / 时区 / 外部编辑器 / 语言）、通知、代码目录授权、健康诊断、关于。其中两页值得单说：

- **通知不是可选项。** P0 是全手动派工，一步跑完就停在「等待你的指派」——没有通知，这个状态只能靠使用者自己想起来回来看。结构照 clowder 的通知页（渠道 + 事件偏好 + 诊断），七类事件每一条都对应一个界面上已经存在的状态；只做应用内与系统通知两个渠道，IM 与移动推送解决的是多人场景。
- **代码目录授权不是「代码目录权限」。** 后者与项目里的「能力边界」语义重叠。全局层只管「这台机器上哪些真实路径被授权过、谁在用、还要不要留着」，四档开关仍然写在项目上。

**三处自我否定，记下来免得再犯**：①「权限档」是「能力边界」的第二个名字，正名不新建页；②「执行策略」是为凑整而发明的容器，逐项归位后不需要存在（并发→adapter 配置，照 multica 把并发挂在执行体上；去重→已在自动化准入检查；时区→偏好设置；额度阈值→运行时页首）；③「运维监控」不整块搬——clowder 那页九个 tab 里，七个在本项目已有归属，真正缺的只有「后台会话」，它变成运行时页首的「运行中」。

**MCP 不单开一页。** 当前模型里 MCP 只有一个来源（能力包声明），没有「自己装第三方 server」这个用法——为一个还不存在的用法建页面就是债务展览馆。但它必须有落点，因为工具静默缺席会让两次执行看起来一样、结论却不同：落点是每个 adapter 配置里的「工具」表，和「本次生效组合」。

**工作流模板并进编组。** 改版前项目面的 Coding / Research / Docs 和能力面的「代码实现 + 独立验证」「调研 + 交叉核对」是同一批东西的两个副本。两个参考项目都没有独立的工作流对象——流程就写在 skill 里。合并时切一刀：步骤与完成要求归 skill，底座硬规则留核心层且**只能加严不能放宽**，「哪条是默认」归项目设置（照 clowder 的「全局默认 Mount Rules，项目可覆盖」）。

**补建了 §3.2.7「本次生效组合」。** 它此前只写在 `docs/design.md` 里，原型中一个字都没有，而同一段承诺的另一半（§3.6.4「本次未进入上下文」）早已建好。三块：不可覆盖的底座 / 这次生效的做法 / 被拦掉的，每一条都带来源。

## V3.22 修版

> 本段只覆盖自动化面。同一批工作里能力面 Skill 详情的改动还没写进这里。

**「Runbook」这个词删掉了。** 它是运维值班手册的行话，而稿子里已经有一个一模一样的东西——新建任务的 `要做什么？包括约束条件，以及什么算做完`。同一个概念不该有两个名字，现在统一叫**任务说明**；原来并排的「任务模板」也跟着改成「建成什么样的任务」，因为它不是可复用的模板库，只是这条规则的建任务参数。

**任务说明改成「人写一句话 → 模型整理成步骤 → 人确认」。** 自动化是无人值守的，明天 09:00 触发时你不在，指令必须预先写死；但让人当场写出结构化的「目标 / 完成要求」又太重。所以创建器里只要求一句话，模型整理成步骤后由人勾选确认。

关键约束是**整理结果冻结成版本**：确认之后每次触发读同一版，不重新生成，改要走「重新整理」并重新确认。每次触发都让模型改写一遍那句原话，规则会静默漂移——三个月后没人说得清它到底在干什么，而这正是自动化最难查的一类问题。概览里保留「你写的原话」与整理版对照，两者都不隐藏。

**Webhook 从每条规则的标配，降级为只属于真有外部入口的规则。** 原来的示例事件是 `dependency.changed`——它假设有个外部系统会在依赖清单变化时 POST 进来，但对一个本机个人工具，自己改了 `package-lock.json` 再绕一圈 HTTP 打回本机触发自己，是自相矛盾的；而且没有任何东西会发这个请求。

现在左栏多了一条 **GitHub issue 分诊**，用 `issues.opened` 承载全部 Webhook 设计（凭证、事件过滤、HMAC 签名、幂等去重、投递审计、重放）。定时规则（依赖检查 / dogfooding 归档 / 推送检查）只有三个 tab，**没有 Webhook 投递层**——让一个多数规则永远是空的 tab 常驻，等于把它摆成一等公民。

这条 Webhook 规则还多摊开两件定时规则没有的事：**入口检查（验签 / 去重 / 过滤）与准入检查（额度 / 运行时）分开记**，前者不消耗额度也不产生 Run——两类原因混成一个「失败」，就再也分不清是别人发错了还是本机跑不动；以及 **issue 正文属于不可信输入**，只作任务上下文，正文里写「请帮我推送到 main」不会让这条规则拿到 push 权限。

**JS 只动了 `pickInList()` 一个参数。** 右侧详情按「规则类型」分而不是每条规则一份，所以定时那三条用 `data-automation-detail` 指到同一份详情上（新增可选参数 `viewValue`）。切规则时强制回到概览——两份详情的 tab 集合不同，停在一个当前详情里不存在的 tab 上会让右侧整块空白。

**验收**：`browser-check.mjs` 85 条过、1 条红（「编组没有把验证步的上下文范围写成字段」，属能力面 Skill 详情那批未完工作，与本次无关，回退本次改动后仍红）；`export-all-pages.mjs` 45 张、0 console 错误、exit=0。自动化那条断言按新结构重写，并补上守住上述判断的几条：任务说明必须留「你写的原话」且写明「不会重新生成」、定时规则不得出现 `Runbook` 与 Webhook 凭证、切规则必须真的换详情、Webhook 规则必须写明入口与准入分开且投递内容是不可信输入。

## V3.21 修版

**能力面的判据换了：它装的是「缺了也照样能干活」的东西。**核心层（项目 · Issue · Room · Run · Artifact · Evidence · 记忆 · 统计 · 设置）缺一个产品就不成立；可选层挂在能力面，任意一个停掉都不影响上面。依赖方向单向：可选层可以调核心层，反向禁止。

这条判据同时给出一条**可判定**的插件准入规则——RSS 订阅缺了照样建 Issue、拉 Room、派工、验收，所以它可以开 tab；adapter 缺了就派不了工，所以 adapter 永远在设置 · 运行时，不进能力面。

改了四处：

1. **「来源」tab 删掉。** 它是自己长出来的第三个 tab：核查后 clowder 的插件组装在 Settings UI（`docs/research/plugin-architecture-dsh-clowder.md` 第 3 节对比表），multica 全文没有插件系统。来源降为表格里一列可点标记，跳设置 · 插件——这正是 §3.2.1 自己写的「来源是治理维度，不是查找维度」。
2. **编组不再是独立类型。** 一条 Skill 填了 `steps` 就是编组（自动带 `#编组` tag），不填就是普通做法；三个 tab 收成**一张表 + tag 筛选**。两个类型会逼人在用熟之后「转换类型」，历史表现跟着断，反而离 ADR 0012 第 5 条要防的「编组积累成身份」更近。
3. **运行时并进设置，一级入口 9 → 8。** 原 §3.7 整体搬进设置的「运行时」组；到达它的主路径本来就是跳转（派工弹窗「结构性不具备」、成果面独立性卡），不是左栏点击。设置同时新增「插件」组——**装一个 CLI 和导入别人写的一套方法不是同一件事**：失败模式不同（起不了进程 / 装了一半），处理动作也不同（去终端敲命令 / 在界面上选一个候选）。分开还有一处白捡的安全收益：**凭据全在运行时组、插件挂载点全在能力面，两处不相交**。
4. **插件的 tab 是声明出来的，不是插件画的。** 新增能力面第二个 tab「订阅」作为演示：`rss-inbox@0.3` 在 manifest 里声明 `kind: table` 加列名加两个动作，宿主用自己的组件渲染。动作只能引用白名单（`host.library.archive` / `host.issue.createWithRoom`），因此「深入拆解」建出来的是普通任务，来源链自动完整——插件自己拼捷径就会绕开它。ADR 0014 的 P12 随之从 C 档改为「声明式 surface 开放（B）/ 插件提供渲染代码仍关闭（C）」。

**JS 只加了两个函数**：`applyPicks()`（一个按钮可以同时切面和切组——设置左栏的 adapter 条目正是 settings + runtime 两个 pick 同时生效）与 `setLibFilter()`（tag 筛选）。

**验收全绿**：`browser-check.mjs` 84 条全过、0 console 错误、exit=0；`export-all-pages.mjs` 44 张、0 console 错误、exit=0。新增 3 条断言（能力面是容器、Skills 一张表 + tag 筛选可用、插件 tab 由声明渲染且动作走白名单），改写 8 条（竖栏不再有 runtime 且必须是 8 项、设置面必须有运行时与插件两组、密钥仍在数据组等）。**顺手修掉两处与本次无关的既有失配**：导出脚本的哨兵张数写着 38 而实际早已是 42；`data-settings-pick="data"` 在左栏和正文各有一处，断言未限定 `.sp-list` 会撞 strict mode。

## V3.20 修版

**自动化从一张静态说明页，补成「定义 → 触发 → 准入 → 普通任务 → 审计」闭环。** 参考 multica 的 Autopilot 后，保留 Runbook、多时间表、Webhook、事件过滤、签名、幂等、投递审计与重放；执行对象改写成 PersonaHub 已有的 Workflow + 执行组合。

右侧按四个 tab 分层：概览回答「它到底会做什么」；触发器回答「什么会让它开始」；运行记录回答「有没有真的跑，以及建了哪条任务」；Webhook 投递回答「外部请求有没有收到、鉴权、去重和分发」。左栏不再把规则与运行历史混在一起。

**没有 `run_only`。** multica 允许直接运行 Agent 而不建 Issue，但 PersonaHub 的可信交付建立在 Issue / Run / Attempt / Artifact / Evidence 的来源链上。绕过 Issue 会形成第二套无法验收与交接的结果体系，所以每次真正执行都建立普通任务；额度或权限挡下时记「未触发」，不会伪造 Run。

新建自动化先定义 Runbook、项目、工作流、执行组合和第一个触发器，保存为暂停并展示预检。启用是第二个明确动作。时间表必须预览至少三次下次运行；Webhook URL 默认隐藏且视为密码，轮换使旧 URL 立即失效；重放创建新记录并指回原投递，不改写历史。


## V3.19 修版

**轨迹详情：右侧详情栏 → 点哪行就在哪行下面展开**（形态照 deepseek-harness 的 trajectory）。

原来选中一行，内容出现在右边那条 340px 的详情栏里。问题是轨迹本身多数时候就是会话的副栏（420px），副栏里再切一栏就只剩两条窄缝——所以副栏尺寸下详情栏是直接 `display:none` 的，**会话面里点行等于没有反应**，详情只有放大成全宽复盘时才够得着。现在详情长在行下面：副栏、放大态、窄屏都是同一套交互，读的时候上一行发生了什么也始终留在视野里。

**分页按事件类型给，tab 组照 harness 实测的原样抄。** 原来三个 tab（结果 / 入参 / 耗时）是照工具调用设计的，套到别的类型上有一半是空的：SYSTEM 没有「入参」，Request 的「结果」是什么也说不清。这一版的 tab 名与顺序不是自己拟的——跑真值探针把 DSH 每一类行点开抄下来的（`ui-reference/scripts/tmp/probe-dsh-detail5.mjs`，需要 `:3080` 在跑；输出留在同目录 `dsh-detail5/`）：

| 事件 | 头部定位 | tab 组 | 这一类真正要回答的 |
|---|---|---|---|
| SYSTEM | `Initial System Prompt` | System Prompt / Tools | 提示原文是什么、这次给了哪些工具 |
| USER | `Turn 1 · Message` | Summary / Preview / Raw / Source | 谁发的、原文、分块原样、请求元数据 |
| ASSISTANT | `Turn 1 · Step 1` | Summary / Preview / Raw | 来自哪个 Request、tokens 拆成 Reasoning/Content、思考与正文分块 |
| TOOL | `Turn 1 · Step 1` | Summary / Payload / Result / Schema / Timing | 入参、结果、这个工具的契约、实测耗时 |
| Request | `Turn 1 · Request #1` | Summary / Timing / Source | **DSH 没有这一类**：它把请求收在 ASSISTANT 的 Source 字段里，PersonaHub 把它单独成行（要标冷启动/续跑），字段沿用 DSH 的 Request Timing 那一组 |

格式也照抄：头部是 kind 徽标 ＋ `Turn · Step` 定位 ＋ 关闭；**Summary 页把其余各页各摘一段**，分节标题带 `›` 指向那一页的全文；字段左标签右值两列，`Tokens` 下面的 `Reasoning` / `Content` 缩进一档；代码块无边框、键与字符串分色。

一次只开一条：同时展开三条，表格就又变回一叠卡片，横着比的能力就没了。筛选、搜索、折叠回合/调用时自动收起——展开的面板留在被筛掉的位置上会变成孤儿。

诊断信息（`run_a3f2` / 原始 session 路径）跟着 DSH 的分层走：不再是详情栏底部那条固定脚注，而是 Request 的 **Source** 页；TOOL 的 Timing 页只写 Started / Duration / Timing source。脚注挂在栏底时看着像整段轨迹共用一条，实际它按调用不同。


## V3.15 修版

**运行时从设置里抽出来，成为一级工作面。** 结构判断见 `docs/design.md` §3.7，模型依据是 `docs/decisions/0012-object-model-simplification.md` 第 2 条。

起因是 ADR 0012 把执行组合从三元组改成 `adapter + 配置 + 模型 + 深度`，并把 adapter 拆成两层：**adapter 是一个 CLI 安装**（版本、能力位、原生记忆、执行位置），**配置是驱动它的一种方式**（base URL、认证、可用模型、额度、项目可用性）。拆完之后这一簇字段比设置面其余三组加起来还多，而它的访问频率是「派工失败时第一个去的地方」——和设置「低频、改完就走」不是一个节奏。

改了五处：

1. **新增 `data-surface-view="runtime"` 一面**，排在统计之后、设置之前。壳复用 `split-surface`（与自动化面、设置面同款），**没有新增一行 JS 逻辑**：左框走既有的 `pickInList()`、tab 走既有的 `setLocalTab()`，只在两个 group 数组里各加了一个 `"runtime"`。
2. **左框是 adapter 列表**，顶部一行「全部」总览。有实体就有列表——这是 §3.4 已经写下的规则（记忆面与能力面没有左框，是因为它们的 tab 是并列的三组东西，不是一棵树）。状态灯复用已有的 `.sp-item:has(> .signal)`，**灯旁必须有文字**，且 per-project 的可用性差异不进灯（灯是全局符号，说不了「这里可用、那里被停了」）。
3. **主面三个 tab**：能力（CLI 安装这一层的能力矩阵，不支持的格子写后果而不是画 ✗）、配置（执行组合全表 + 额度池 + 凭据）、诊断（每一行都带可执行动作）。
4. **设置面收敛为低频三组**（任务前缀与标签 / 数据 / 其他），原来的「Adapter 与执行组合」「模型与凭据」两块整体搬走。
5. **额度按 adapter 配置分池，不叫「账号」**。派工弹窗里逐模型标百分比的老 bug 一并修掉——claude 的 opus 与 sonnet 同属一份配置，不可能同时剩 62% 和 88%；现在每行标它属于哪个池。

**OpenCode 挂了两份配置**（`coding-plan` / `official-api`），这是四元组存在的那个真实场景：同一个 `deepseek-v4-pro` 两条路，一条吃套餐额度、一条按 token 真花钱，失败模式也不同（前者撞限流，后者不限流但烧钱）。合成一行会让历史表现被平均，那张表就不能再用来做判断了。

**防护条款写进了断言。** 运行时面不出现「设为默认 / 优先级 / 置顶 / 重命名」——判据是「改了它之后『派工时该选谁』的答案会不会变」。会变的就是偏好，偏好会让 adapter 长回 ADR 0012 取消掉的「AI 成员」。

**验收已跑。** 本机跑过 `browser-check.mjs`（**73 条全过、0 console 错误、exit=0**），确认结构可用。（当时还有一份截图导出脚本，每轮重刷三十多张 PNG 只是 diff 噪声；V3.26 起整条规则已删除，见「验收」一节。）

断言按新结构新增 3 条、改写 4 条：

- 新增：左框是 adapter 列表且灯旁必须有文字、能力矩阵横着比且不支持的格子写后果、同一模型两条路必须是两个执行组合。
- 改写：竖栏清单与顺序（运行时在统计之后、设置之前）、执行组合表改指运行时面且表头要能看出四个维度、额度与凭据两条从设置面搬到运行时面的配置 tab。
- 顺手修掉一条**与本次改动无关的既有断言 off-by-one**：「统计的任务行要跳回任务模块」取的是 `.dl-row` 的第一条，而第一条是合计行——合计行按设计不带页码、不随翻页变化，本来就不该跳转。改成 `.dl-row:not(.dl-total)`。

**这一条已由 V3.21 结清：** V3.13 的「来源」tab 最终没有实现，而是被删掉——已装能力包与插件归设置 · 插件组，来源在 Skills 表里只是一列可点标记。


## V3.12 修版

**「用量」改造成「统计」，额度搬回设置 · 运行时。** 结构决策见 `docs/decisions/0017-usage-accounting-and-stats-surface.md`。

起因是两个问题撞在一起。一是维度：multica 的 dashboard rollup 键是 `(date, workspace, agent, project, model)`，前端主表是智能体排行榜；clowder 聚合到猫。这两个维度成立是因为 agent / cat 是**可治理的对象**——看完排行榜能去关掉它。而 ADR 0012 已经把这一层取消了，照抄只会得到一张每行都没有对应动作的表。二是位置：原来的用量面通篇是「还能派几次」，可 ADR 0012 第 3 条明明把剩余额度定义成 Runtime 的字段——额度当时被放错了地方，而真正的 token 账本一直不存在。

改了五处：

1. **结构照记忆面，不照任务面。** 任务模块是「列表 → 单任务 tab → tab 下内容」，因为任务是实体；统计没有实体可列，选的不是哪一条而是**看哪一类问题**，所以进来就是 tab 页，没有左列表。只留两个 tab：用量、失败。
2. **主维度换成「去向」。** 详情表主维度为任务，第二维度为用途（实现 / 验证 / 返工重试 / 上下文重传）；模型与执行组合退为同一张表的第三、四个维度，定位是**单价基准**而非排行榜——更换组合会改变结果质量，这属于使用者的质量判断，不是降低成本的手段。四个维度共用一张表，通过卡内切换，不新开页面。
3. **实付与订阅等价分成两个数字。** 订阅账号跑掉的 token 不产生新支出、只消耗额度；API Key 账号不消耗额度、但每个 token 都是真钱。等价一行标注「非实际账单」——clowder 的价表注释里那句 *never present them as actual billing* 是付出代价后才写下的。组合表另加一列「计价来源」：adapter 自报为权威值，本地价表折算为估算值，合并为单一数字后将无法区分可信部分。
4. **年度热力图作为趋势卡的第三种形态**（逻辑与样式照 GitHub 贡献图）。不做成页首常驻横幅：那样它固定显示一年，而同屏 KPI 跟随周期筛选，页面上会并列两个时间口径的数字。形态由周期决定——近 7 天只有按天、近 30 天有按天 / 按周、近一年有按周 / 热力图并默认落在热力图；**不可用的形态置灰而不是隐藏**，藏起来等于替使用者决定他不需要它。
5. **详情表改分页**（每页 10 条，共 17 个任务），**合计行置顶**——紧贴表头、不带页码、不随翻页变化。原来的「其余 12 个任务」聚合行删掉：它存在的唯一理由是让各列能加到 KPI 上，但它点不动、不可下钻；合计行做同一件事，而且是正式数据表的常规做法。分页最容易翻掉的正是对账关系，置顶的好处是这个总量在翻页前就已经读到了，不必翻到底再找。不设「本页合计」——页内小计不回答任何问题，只是把表撑高。

   **Token 拆成缓存命中 / 未命中两列**：命中是从 prompt cache 读取、按折扣价计费的部分，未命中是本次真正处理的输入加上全部输出（输出永远不进缓存，所以算未命中），两列相加等于该行全部 token。四个维度的命中 / 未命中合计完全一致（79.0M / 52.6M），因为它们本来就是同一批 token 的四种切法。这一刀对应可调的旋钮：「验证」按 ADR 0009 冷启动，命中天然低；「上下文重传」命中率只有个位数，它的未命中量就是把上下文范围收窄能省下的上限。

   **同时修掉一个自造数据：**「返工 62%」原来挂在**状态**列里，而那一列其余的值全是任务状态——一个状态列里混进一个算出来的比例，读者无从核对它怎么来的（它当时确实也没有任何出处）。现在独立成「返工占比」列，口径写在表下：该任务 `step_kind='返工重试'` 的 token ÷ 该任务全部 token，17 行按 token 加权后是 17.98%，与「用途」维度写的 18% 对得上。
6. **术语统一为正式用词。** 分布卡定名为**详情**；「还能派」→「可派次数」、「上下文重发」→「上下文重传」、「权限被拒 / 超时 / adapter 报错」→「权限拒绝 / 执行超时 / adapter 错误」、「不是账单」→「非实际账单」、「跑了但失败」→「已执行但失败」。说明段落同步改写，去掉口语表述。
7. **额度整段搬到设置 · 运行时**（额度池按账号、深度折算、告警阈值），派工弹窗里「额度不够」的理由随之改指那里。统计面从此**全屏没有一个实时数字**。

**一处刻意的不一致。** 本项目一直守「未知不填 0、未计入单独成段」（V3.11 的轨迹条就是这么画的）。这一版**没有**为「adapter 没回报 usage」造视觉语言：没有 KPI、占比图里没有那一段、热力图上没有第六种记号。理由是三个 adapter 里已确认 Claude Code 会回报，后续只会适配更多 CLI，为一个可能不出现的情况造一整套记号，成本大于它防的问题。数据层仍然存 NULL 不存 0——那一列不要钱，而 0 与「没测到」一旦在写入时合并就再也分不开。

**数字是对得上账的。** 三个周期（7 / 30 / 365 天）的 KPI 与四张分布表由同一份 365 天的伪随机数据生成，每张表的每一列都能加到对应周期的 KPI 上，末行吸收舍入差。原型里数字对不上账，是评审第一个会抓的东西。

**验收：** `browser-check.mjs` **74/74 通过，零控制台错误**。断言按新结构改写并新增：统计面结构（无左列表 / 两个 tab / 筛选在 tab 行右侧 / 全屏无实时额度）、趋势卡的周期—形态门（热力图只在近一年可用且默认落在它）、明细回任务轨迹、详情表分页与置顶合计行（首页 10 行、合计不带页码且与 KPI 对得上）、失败页口径、额度落位。



## V3.11 修版

**一轮设计审计（redesign skill）＋ 轨迹概览重做。**

审计出来最大的一条是**字号整体塌在 8–11px**：`v3-layout.css` 里 ≤9px 的声明有 134 条、10px 有 133 条、11px 有 113 条，而 ≥14px 的只有 20 条——列表表头是 9px，扫不出列名。统一抬了一档（7/8/9→10-11、10/11→11-12、12/13→13-14，共 607 处），写死的 `--cols` 列宽跟着按比例放大，否则抬完字号就截字。

其余六项：

1. **补三处焦点环**。新建任务的标题与描述、轨迹搜索把 `outline` 抹成 `none` 又没给替代物——弹窗里仅有的两个输入，键盘进去看不出焦点在哪。（`<button>` 本来就有，multica-theme 里配过。）
2. **输入框上缘补 40px 渐隐**。它是 `sticky` 的，内容从下面滚过去；原来上缘是硬边，正文被齐刷刷切断，看着像页面到此为止。
3. **验收的三张统计卡压成一条统计条**。三个边框三块内边距换三个数字，数字才 16px 卡片却占 60px 高；共用一个外框、段间一条竖线，高度减半，筛选行为不变。
4. **健康度的主动作从黑底白字降为强调色描边**。五条债务各一个主动作，竖排就是五个等重的黑块。黑色实心留给页面级唯一主按钮。
5. **`chevron` 从字符 `⌄` 换成 SVG**。U+2304 在多数字体里不贴基线，顶栏「我的工作区 ⌄」的箭头掉到了标题下一行。
6. **清掉 28 条死规则**（`collaboration-dock` / `dock-recipient` / `primary-nav`），都是 V3.3/V3.4 删掉的东西留下的。

**轨迹概览：三泳道 → 一条占比分段条。** 三泳道答的是「何时发生」，可下面那张事件表本来就逐条记了时间戳，概览这一层是重复的；而且它的高度会随字号漂移（这一版字号一动就撞穿了「概览条不该抢表格位置」那条断言）。现在改答**「时间花在哪」**：一条满宽条按耗时占比分成四段，高度固定在 8px 条 + 一行图例，与栏宽和字号都无关。

这一条的关键是第四段——**「未计入」单独成段并画成斜纹**：计时未知的调用、仍在执行的调用、以及调用之间的空档，全部不摊进前三段。摊进去就是把不知道的时间当成知道的，和原来「计时未知画空心，绝不伪造 0ms」是同一条原则换了个画法。点任一段（或图例）把下面的表格筛到这一类，再点一次取消。

**一个花了三轮才定位的坑：** 新加的 `task-trace.png` 里概览条一直不在画面里。先怪副栏没滚到顶，重置了 `[data-aside]` 及其子元素——没用；`scrollIntoView` 反而把工具条顶出去了。逐级打印滚动祖先才看清：滚动的是 `.task-pane`，它是副栏的**祖先**不是后代。教训——**重置滚动要往上走，不是往下走。**

## V3.10 修版

**记忆从「一张能看的清单」变成「能治理的库」。** V3.9 的记忆面已经有强度（stance）和 type，缺的是「这条现在还算不算数、错了怎么撤、库整体有没有烂掉」。按 ADR 0016 补三样，全部沿用 V3.9 的列表范式，不新增布局原语：

1. **三根轴摆成三列，不合成一个「可信度」**：`强度`（谁背书）/ `验证于`（何时验证过）/ `引用`（被用过几次）。被用得多不等于被验证过，被验证过不等于你认可——这句话直接写在列表末尾，因为三个数字并排摆着，读者仍会自己把它们加起来。`claimed` 行的「验证于」显示「— 未验证」：**引用次数永远不会自己写进验证时间，也不会自己提升强度。**
2. **状态成为一列**（在库 / 待复核 / 已退役 / 已遗忘），配独立的状态筛选组，与强度筛选分开两行——两根轴不共用一个筛选器。待复核行写明是哪一版证据失效了（`@2 → @4`）、它已退出召回、且引用 9 次不能替它续命；退役行压暗但保留来源；遗忘行内容位是「〔内容已按授权清除〕」，次行是墓碑，并注明**遗忘前必须先退役**。来源包与一跳关系放次行，不另开页面。
3. **新增「健康度」tab**：过期 / 未裁决积压 / 孤立 / 冲突 / 未验证债五项债务，**每一行右侧都必须有可执行动作**，批量操作先试运行。另有写入侧三格（入库 / 写入被拒绝 / 建关系失败），并写明「被拒绝长期为 0 不一定是好事——也可能是这条路根本没被走过」。

任务面的「资料 · 输入」补了一段**本次未进入上下文**：压暗但在场，写明过滤了几条、因为什么。过滤不能是静默的，否则实现者的自述会经由记忆以「项目事实」的身份绕回来。

**一个踩到的坑：** 健康度的行带动作按钮，一开始照知识库写成了 `<button class="dl-row">`。嵌套 `<button>` 会被解析器提前闭合外层，四个动作按钮直接跑出网格，铺成整行黑条。带动作的行必须是 `<div>`（待确认面本来就是这么写的）——**列表行是不是按钮，取决于它里面有没有按钮。**

## V3.9 修版

**记忆与能力改用列表，不再用卡片。** 卡片的问题不是好不好看，是**管不动**：每张卡自带边框和内边距，同一个字段在不同卡里的位置对不齐，扫十几条就得逐张读；条目上百之后更没法比较。列表把字段压成对齐的列——横着能比、纵着能扫。

四个列表共用一个 `.data-list`（表头 + 行，列宽由 `--cols` 定）：

| | 列 |
|---|---|
| 记忆 · 待确认 | 强度 / 内容 / type / 来源 / 时间 / 操作 |
| 记忆 · 知识库 | 强度 / 内容 / type / 层级 / 来源 |
| 能力 · 编组 | 编组 / 步骤 / 上下文范围 / 工作流 / 状态 |
| 能力 · Skill | 强度 / 做法 / 工作流 / 触碰路径 / 命中 / 最近 / 操作 |

引文、警告、编组的理由放在次行（`grid-column: 2 / -1`），限宽 900px——列表可以铺满 1318，但要读的句子一行 100+ 字读不动。

**列宽必须写死。** 最后一列原来写 `auto`：表头是「操作」两个字、数据行是四个按钮，宽度不同，`1fr` 把差值吃掉，整行的列就和表头错开了 186px。列表的意义全在对齐，这一条有断言守着（逐列比对表头与数据行的 x）。

**两面的排版也彻底对齐了。** 之前差在两条只列了 `memory` / `projects` 的选择器上：能力面不在里面，还在吃 `.surface-content` 的默认值（`h1` 29px、`small` 9px 蓝色大写），于是 tab 行比记忆面低 15px，来回切整页往下掉一截。卡片则是 14/16 内边距对 12/14、编组标题 16px/700 对记忆 12px/600。现在断言逐项比对两面的 `header small` / `h1` / `.mem-tabs` 的字号、字重、颜色、y 坐标与高度。


**一个结构性 bug，值得单独记：** 改工作流卡片时用非贪婪正则截 `<div class="pj-list">…</div>`，它停在了第一张卡片内部的 `</div>` 上——新卡片插进去、旧卡片的尾巴留在后面，项目面凭空多出两个闭合标签，把 `<main>` 提前关掉。后果是**记忆 / 能力 / 自动化 / 设置四个面全被挤到 `app-shell` 底下**，`absolute; inset: 0` 直接盖住整条竖栏：页面看着没事，就是点不动。教训还是那条——嵌套结构不要用正则截，按标签配对扫。现在有两条断言守着：每个面必须挂在 `surface-host` 里且 `x >= 50`。

另一个反复出现的坑一次性堵死了：在裸类名上写 `display` 会盖掉 `[hidden]`（`.trace-item` / `.baseline-gate` / `.claim-state` / `.project-manage` / `.project-pane.files` 都栽过，表现是切 tab 时上一个视图的内容还留在页面上）。现在有一条全局兜底 `[hidden] { display: none !important; }`，外加一条断言扫全站。


1. **四个视图的正文宽度与左边界统一**。逐个类名地限宽总会漏掉一个：验收的 `.document` 用的是 `width: min(850px, calc(100% - 56px))`，`width` 会盖过 `max-width`，于是它比其他三个窄 22px，切 tab 时正文左边界跳一下。现在统一交给 `--content-w`，并直接约束 `pane-main` 的所有直接子元素。概览也跟着居中。
2. **顶栏去掉「均衡 / 阅读 / 协作」三档**。这三档是 V3.0 为「舞台 + Dock 两栏怎么分」设计的；Dock 已按 ADR 0012 取消，副栏也有自己的折叠开关，它就没有对应的东西可调了。
3. **项目面工具条改小图标**（照 multica 的 `size-7` 范式）：28×28 无边框方按钮，16px 线性图标，hover 才出背景，带 `aria-label` 与 `title`。
4. **目录树的状态标记统一靠右**，并改用 git 那套字母 `M / A / D`。跟在文件名后面时每行位置都不同，扫不出「哪些改过」。
5. **项目面的知识 / 工作流 / 设置三个视图重写**。这三块原来是从别处整段搬来的：知识用了记忆面的两列网格、工作流用了一份这里根本没有的样式、设置只剩一个裸 `git-block`。现在各自有本面的排版——知识带「为什么」，工作流画出步骤链与 Done 判据，设置分三组并写明 `git push` 需逐次授权、项目边界只能更严不能更松。

## V3.8 修版

1. **项目面布局修复**：第 43 节留了条给旧项目面的 `grid-template-rows: minmax(0,1fr)`，让 tab 行被 `1fr` 撑成 373px，整块内容掉到下半屏。删掉即可。左框条目带项目徽标时改成三列，否则徽标会占掉 `1fr`、文本挤到右边。
2. **目录状态标记改英文**：`改` → `M`，沿用 git status 的 `M / A / D`。中文标记和旁边的英文路径混排很跳，而 git 那套是现成的共识。
3. **工具条按钮去图标**：`＋📁` / `＋📄` → `新建文件夹` / `新建文件`。图标叠加号要认两次才知道是什么。
4. **会话分类移回左框**（独立 / 项目），样式同任务左框的置顶 / 最近。分类是「在这一堆里挑一个」的维度，属于列表那一侧；放在右侧视图行上会让人先看右边再回左边找，动线是拧的。
5. **会话居中，输入框贴合会话栏**。输入框是跨视图常驻的（切 tab 保留草稿），所以挂在 `editor-area` 上；但视觉上必须对齐正在说话的那一栏，否则读起来像页脚。轨迹副栏展开时它让出 440px，轨迹放大时它随会话一起收起。

## V3.7 相对 V3.6 的七项变更

右侧留白此前是靠 `max-width` 限宽掩盖的。这一版给出一条规则，而不是五个补丁：

> 每个视图 = **主栏 + 副栏**，副栏放「看主栏时最想同时看到的那一份」，都能折叠；折叠后主栏拉满。轨迹本来就是这个形态，等于把它推广到全部视图。

1. **概览副栏＝活动**：任务级事件——状态迁移 / 基线变更 / 派工 / 记忆写入。概览要答「下一步给谁做什么」，缺的那一半是「这几天发生过什么」。它比轨迹粗一层：只记状态变化，不记每一次工具调用。
2. **会话副栏＝轨迹**：一边说话一边看它在干什么。默认展开，收起后会话拉满。
3. **轨迹不再是独立 tab**——同一份数据留两个入口只会让人犹豫该点哪个。副栏按 <kbd>⤢</kbd> 放大成全宽，工具栏、搜索、折叠、详情栏全部回来，功能和原来那个 tab 一样。视图从五个减到**四个**：概览 / 会话 / 验收 / 资源。
4. **验收副栏＝大纲**：主张目录 + 每条主张下挂的用例 + 状态符号，点了跳转并高亮。底部单列「**未被覆盖的要求**」——GSN 的 `UndevelopedGoal`，长文里最容易划过去的就是它。
5. **资源改成清单 + 预览**：点文件右侧直接出内容，代码带行号与增删标记，Markdown 直接渲染。和项目面同一套组件。清单留在原位，方便连着看好几个文件。
6. **两种打开方式并存**：验收正文里的引用是「顺着读下去」，占满舞台并靠返回条回来；资源清单是「挨个看」，就地预览不跳走。
7. **新建任务去掉标题字段**——让人先想「叫什么」是在浪费他刚起来的那点动力。标题由第一轮执行读完内容后总结，随时可改。

## V3.6 相对 V3.5 的六项变更

1. **会话面改用任务面的骨架**：左列表 + 上 tab + 消息流 + 漂浮输入框。之前是独一份的样式，和任务面对不上，等于要记两套。tab 切的是**独立会话 / 项目会话**，左列表跟着只显示这一类。
2. **我发的消息靠右**。左右分边比头像更快区分「谁说的」——扫一眼边界就知道，不用先看清头像里那个字。
3. **记忆与能力还原成纯 tab 切换**。左框 + 右主体这个外壳不是哪儿都合适：这两个面的内容本来就是两组并列的东西，左框只是多一层点击。项目和会话有「先选哪一个」的需要，才留左框。
4. **项目面左栏改成项目列表**，文件 / 知识 / 工作流 / 设置四个 tab 移到右侧上方。文件 tab 内仍是 GitHub 式的左树右预览。
5. **新建任务照 multica 重做**：一条竖向表单——标题大字 → 描述 → 一排属性 chip（项目 / 标签 / 执行模型），没有一行行的 label + 控件。**工作流这一轮先不加**。
6. **建完直接进会话开跑**。既然在弹窗里已经选了执行模型，就说明现在就要它动起来：提交后落到新任务的会话视图并把第一轮派工写进流里，而不是丢回概览再点一次。

## V3.5 相对 V3.4 的十二项变更

1. **六个工作区面统一成「左框 + 右主体」**：左边是这一类东西的清单，右边是选中那一个的全部内容。此前各长各的样，每切一次都要重新找。
2. **独立会话成为一级对象**。不挂任何任务的讨论此前无处安放，硬塞在某个 Issue 下不合适。会话面分「独立会话」与「来自任务」两组；独立会话**不产生验收、不写记忆**，要落成结果走「转成任务」。
3. **项目面改成 GitHub 式**：左树右预览，点文件不跳走。代码带行号与增删标记，Markdown 直接渲染——方便连着看好几个文件。
4. **任务标识与元信息独立成第一行**，视图 tab 落到第二行。标题和 tab 挤在一行时标题被压成一小截，元信息只能塞进正文里重复七遍；拆开后元信息只有一份。
5. **标签收进下拉**。横排 chip 的问题是标签一多就折行，左栏高度跟着标签数量变。
6. **能力库改名「能力」，只留编组与 Skill**。「做法」是我自造的中文名，和 Skill 是同一个东西——统一叫 Skill，并给它能真正管理的位置：什么时候注入、来源与命中记录、启停。
7. **执行组合下沉到设置**，作为 adapter 的配置检查。它半年不变一次，却被放在天天要用的入口里；这张表是**检查结果**（装了哪个 adapter 就有哪些组合），不是配置项。
8. **竖栏分两组**：任务 / 会话 / 项目 / 自动化在上，记忆 / 能力 / 用量 / 设置沉到底部一排。用量单独成面而不是塞进设置——额度是天天要看的**约束**（今天还能派几次 high），不是配置项。
9. **新建任务弹窗重做**：居中，只问目标与归属（项目 / 工作流 / 标签），**不在这里挑执行组合**——建了任务不等于马上开跑，谁来做在任务里看着目标决定更准。输入框改漂浮态。
10. **设置里补「任务前缀与标签」**。任务编号一发出去就被 commit message、证据文件名、导出文件名引用，所以改前缀做成要确认的操作：会重命名全部已有任务，而写进 commit 和证据里的旧编号不跟着变，只留一条别名记录。标签明确**不参与派工判断**，只是筛任务和圈自动化规则范围的一层；被规则引用的标签删不掉。
11. **「成员选择器」改成「执行组合选择器」**。ADR 0012 取消了成员这一层，弹窗却还在按人选：名单来自能力库、行标题是「独立验证员」这种角色名。现在列的是设置里检查出的 7 个执行组合，名单不在弹窗里增删。同源判定跟着改为**看模型不看深度**——同一个模型换个深度再验一遍，验的还是它自己；额度不够跑完一次 high 也是不能选的理由之一，且理由指回用量面。
12. **「模型与凭据」照 clowder 的账户页做成真页面**（此前只是个 toast）。分两组：**CLI 登录态**由 CLI 自管，PersonaHub 只做只读检查、给终端命令，不代管 token——代管等于把三个 CLI 的凭据格式和过期策略全抄一遍；**API Key 账号**才是 PersonaHub 自己存的，单独成表。密钥落在明文 SQLite 列上，这条风险写在界面里而不是只写在文档里。

## V3.4 相对 V3.3 的七项变更

1. **左栏按组织维度分类，不再按状态分组**（照 clowder `ThreadSidebar`）。原来的「需要你处理 / 正在进行 / 最近完成」三组有个硬伤：状态天天在变，同一个任务今天在这组明天在那组，位置就记不住。换成**置顶 / 最近 / 项目 / 收藏**——位置由你决定，状态退回条目里的圆点。「项目会话」入口删除；**＋ 新建任务**提为顶部主按钮；任务可以打标签，分类 × 标签取交集。
2. **成果与资料合并为「资源」**，任务面从六视图收敛到五视图。拆成两个 tab 时，一个既被读过又被改过的文件会两边各出现一次；合成一个面、按**方向**分段（产出 / 输入）就没有这个问题。
3. **项目面换成 VS Code 式目录树**：左项目列表 + 右可折叠树，支持折叠、全部折叠、按名称过滤。原来把整个资源库平铺在项目卡片里，目录一多就放不下。
4. **记忆面拆成待确认 / 知识库两个 tab**（照 clowder `MemoryNav`）。待确认是 inbox，也是**唯一**能把知识升为 `confirmed` 的地方——写入触发器来自验收事件，但升级必须人点；知识库的核心是检索 + 按 stance 过滤。
5. **能力库改列执行组合**，不再是「AI 成员」卡片——ADR 0012 已经取消那一层。表格是 adapter × 模型 × 可用深度 × 额度；编组把每一步的**上下文范围**写成结构化字段，否则「独立验证」只是句口号。
6. **自动化每条规则摊开完整链路**：触发 → 在哪个项目 → 建任务并按哪个工作流 → 派给哪个执行组合 → **能力边界**。缺权限时规则停下来问，不会自己把权限降下来跑；执行历史把「未触发」和「跑了但失败」分开记。
7. **清掉四处残留的旧一级导航**。加竖栏时 `library` / `automation` / `settings` / `projects` 里的 `primary-nav` 没删干净，同一组入口在页面上出现了两次。
8. **轨迹概览换成三泳道**（照 harness 的 `TrajectoryTimeline`）：Chrome Network 风格的**输入 / 模型 / 工具**三条，记录按 kind 投影（`laneFor`：tool→2、message→1、其余→0）。此前那版逐调用一行的甘特图太占地方。模型条按 TTFT 切成两段；**计时未知画空心，绝不伪造 0ms**。（V3.11 已改为单条占比分段条，见下。）

## V3.3 相对 V3.2 的六项变更

1. **取消协作 Dock。** 会话降为任务面的一个视图；输入框常驻任务面底部，看着成果就能下指令，切视图保留草稿（`docs/v3.2-claim-evidence-proposal.md` §10）。
2. **任务面六视图**：概览 / 会话 / 验收 / 成果 / 轨迹 / 资料。tab 上的数字是**需要人工介入的件数**，不是内容总数；一件事只在它的处理位置计数。
3. **执行单位改为 `adapter + 模型 + 深度`**（如 `codex-gpt5.6-high`），取消「AI 成员」这一层。**选择分两步**：先从运行时的真实模型清单里挑 adapter × 模型，再用滑条独立调深度——不做固定组合，否则等于又造一遍刚取消的「成员」。档位由模型决定（只支持一档的模型滑条自动禁用），额度按深度折算成可用次数（ADR 0012 第 2 条）。
4. **上下文范围在派工时显式选**：全部 / 只给结果 / 只给目标。改回「全部」会触发降级提示——这次验证不算独立（ADR 0012 第 4 条）。
5. **项目照 multica 进左栏**，与任务并列；任务列表里的「项目 ▾」只作筛选，管理项目走 ▦ 入口。游离态任务归「未归类」。
6. **labels 代替父子 Issue**，「协作现场」退役为视图名（ADR 0012 第 8 条）。
7. **搜索只保留一处**：顶栏主搜索框接管左栏的任务筛选，左栏不再有第二个搜索入口。
8. **顶部只剩一行 chrome**：任务 tab 条取消，当前任务的标识并进视图行左侧；切任务回左栏点（左栏本来就常驻任务列表）。
9. **概览重设计为决策面**：只放别处答不了的四段——目标 / 等你决定什么 / 现在什么情况 / 下一步建议 + 一行简况指针。三卡与主张树全部归验收，两个视图零重叠。
10. **发给不再选会话**：在哪个任务下就发给该任务的活跃会话，选择器只剩执行组合。

## V3.2 相对 V3.1 的核心变更

可信度锚点从「证据」换成「主张 → 论证 → 证据」：三卡改为**已独立验证 / 有证据待验证 / 需要你处理**（三数之和等于主张总数），只用 `✓ ◐ ⚠` 三个符号，精确状态由每条主张的「结论」文字行承担——**同源验证拿不到绿勾**。证据用仓库里的天然标识，不新造编号体系。理论依据见 `docs/decisions/0010-claim-evidence-structure.md`。

## 打开方式

双击 `index.html`，无需安装依赖或启动服务。

## 一句话结构

```text
竖栏 58px    上：任务 / 会话 / 项目 / 自动化　下：记忆 / 能力 / 用量 / 设置
左栏 248px   置顶 / 最近 / 项目 / 收藏 + 标签下拉 + 任务列表 · 无搜索框
会话面     同任务面骨架：左会话列表 + tab（独立 / 项目）+ 消息流 + 输入框
项目面     左项目列表 + 右 tab（文件 / 知识 / 工作流 / 设置）
记忆 / 能力  无左框，直接 tab 切换
用量 / 设置  左框 + 右详情：用量按账号分额度池；设置分 运行时 / 工作区 / 数据 / 其他
任务面 剩余   第一行 任务标识 + 元信息；第二行 概览 / 会话 / 验收 / 资源
             每个视图 = 主栏 + 可折叠副栏（活动 / 轨迹 / 大纲 / 预览）
             底部常驻输入框：发给 ▾（会话 · 执行组合）+ 上下文 ▾
```

`docs/design.md` 是设计基线；实现可以调整像素与动效，但不得悄悄改变那里确定的对象归属、状态含义和操作影响范围。

## 相对 V2 的六个改动

1. **主舞台是成果面**，不再是「打开任意对象的通用画布」。Evidence Summary 从产出树下的三级页面升格为舞台的固定结构：系统记录 / 实际变化 / 验证独立性三卡 + 完成要求逐条挂依据 + 执行计划。
2. **舞台是主角**。V2 是画布 39% + Dock 39%（各 565px）谁都不是主角；V3 是舞台 55% + Dock 400px。
3. **左栏并成一栏**：48px 图标活动栏并入，一级入口带文字；四棵并列对象树收敛为**单一可折叠列表**（V3.0 曾用「任务 / 资源库」两 tab，V3.1 删除，见 design.md §4.5）。资源库退化为该列表下半部的三个分节（文档与素材 / 产出 / 知识），代码目录不常驻。
4. **删掉 IDE 隐喻**：编辑器标签页排、底部执行面板、底部状态栏。舞台单例，靠面包屑返回。
5. **Dock 概览 tab 拆解**：高频的「谁在做 / 第几步 / 卡在哪」上提为常驻两行；「详细执行计划」下沉到成果面。
6. **项目级会话**：没有选中任务时 Dock 显示不绑定任何任务的项目会话，可以直接问「这个项目现在什么情况」。

## 可点击范围

1. 左栏一级入口：两组八个——项目工作台 / 会话 / 项目 / 自动化，记忆 / 能力 / 统计 / 设置。
2. 任务列表：范围切换（当前项目 / 全部项目）、项目会话入口、任务与协作现场的父子层级、重命名 / 归档 / 删除菜单。
3. 资源库：文档与素材、产出、知识三组。
4. 成果面：三卡状态、成果本体（文件与阶段成果）、完成要求逐条追证据、执行计划；点文件进入全文视图后可返回任务。
5. 协作 Dock：任务会话 / 协作现场、`@执行组合` 派工、固定指派草稿、暂停与恢复（持久横幅）、固定当前协作现场。
6. 顶栏布局三档：均衡 / 阅读（Dock 收成竖条）/ 协作。

## 验收

```bash
# 任意静态服务指向本目录，默认端口 4179；也可以直接
# PERSONAHUB_V3_URL="file://$PWD/index.html" node browser-check.mjs
node browser-check.mjs        # 97 条断言，直接对应 docs/design.md 的结构判断
```

**不再导出截图**（V3.26 起）：设计稿是可交互的，直接开 `index.html` 就行。截图既会立刻过期，又要靠一份导出脚本跟着页面结构一起维护——它维护的是一份比原件更差的副本。`export-all-pages.mjs` 与本目录的 `shots/` 已删除，`browser-check.mjs` 里的截图调用一并去掉，验收结果改写到 `browser-check.json`。

`browser-check.mjs` 的断言写成「design.md 的某一条被改掉就会红」，例如舞台宽度占比、实现与验证两列是否等宽、围栏右侧是否标了冷启动、暂停横幅在 toast 消失后是否仍然可见。当前 **81 条全过**，0 控制台错误。合并远端 V3.12–V3.18 时，按记忆设计 §10.4 的分期把语义召回探针改回**置灰预留**（`semantic_offline` 属阶段三，接入前置灰可见、不假装可用），「两路融合在跑」一行同步改回阶段一的单路 RRF 口径。V3.12–V3.18（统计 / 能力库 / 运行时 / 派工三档）与 V3.19–V3.20（轨迹行内展开 / 自动化闭环）两边的断言全部通过。

`verify.mjs` 已删除：它是 V2 的静态标记检查，其中「预览与 Room 等宽」等断言与 V3 的结论相反，保留会误导。

## 待观察的判断

`docs/design.md` §7 记录了三条本轮明确选择、但需要真实使用验证的决定，各自写明了风险、切换信号和备选方案：

- §7.1 「开始中心」降级为范围切换（同时推进 2 个项目时可能不够用）
- §7.2 协作 Dock 常驻不折叠（信任建立后可能变成浪费）
- §7.3 资源库收成左栏的三个可折叠分节（产出的历史检索路径变长一层）

## 定位

本目录是 PersonaHub 产品结构、对象层级和交互语义的设计基线，不是生产实现代码。

视觉参考许可与声明见 `LICENSE.multica` 和 `NOTICE.multica`。
