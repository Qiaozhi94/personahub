# PersonaHub 交互设计版本

| 版本 | 定位 | 页面状态 | 维护状态 |
|---|---|---:|---|
| [V2.0](./personahub-v2.0/) | 项目优先工作台的原始设计基线 | 17 | 历史冻结 |
| [V2.1](./personahub-v2.1/) | 补齐最终态对象、状态与协作交互 | 24 | 历史冻结 |
| [V3.0](./personahub-v3.0/) | 以「成果面」为主舞台、协作会话常驻右侧；删除 IDE 隐喻 | 26 | 历史冻结 |
| [V3.1 → V3.44](./personahub-v3.1/) | 取消协作 Dock，改为左 issue + 右任务面（五视图）；可信度锚点改为主张—论证—证据；执行单位改为 adapter+配置+模型+深度（V3.14 起，ADR 0012）；独立会话成为一级对象并与任务面共用骨架；每个视图 = 主栏 + 可折叠副栏，轨迹并入会话（四视图）；设置补齐任务前缀/标签与模型凭据；记忆补上状态、三轴与健康度（V3.10，ADR 0016）；轨迹概览改单条占比分段条（V3.11）；「用量」改造为「统计」、额度搬回运行时（V3.12，ADR 0017）；能力库扩成编组 / Skill / 来源完整一面（V3.13，ADR 0018）；运行时从设置抽出、独立成带 adapter 列表的一级工作面（V3.15）；派工三档改为建议 / 可选 / 不可选（V3.18）；轨迹详情改为行内展开、分页按事件类型给（V3.19）；**自动化补全创建、触发、运行与 Webhook 投递闭环，并坚持所有执行回到普通任务证据链**（V3.20）；**设置面一级目录只放类别、补齐偏好/通知/目录授权/健康诊断/关于五页，工作流模板并进编组**（V3.23）；**运行时按执行机器重构——一台机器一个 tab、adapter 是表、点行弹右框，并删掉五样不再回答问题的信息**（V3.24）；**额度只报上游给的时限额与周限额，不再折算成「还能派几次」**（V3.25）；**adapter 表提到运行中之上、右框取消内层 tab 改为四块线性分区、属性收进各自的配置卡**（V3.26）；**按增删改查补齐配置对象的操作与弹窗，「弹层」统一改称「弹窗」**（V3.27）；**设置与运行时按作用域重切——运行时重回一级面并改成盘点口径、设置收成「应用 / 工作区」两组七项、新增代码仓、统计「失败」扩为「监控」、MCP 提为能力面 tab**（V3.28）；**按正式交付标准重过一遍——机器改左栏列表、运行中并入统计 · 监控、取消「能力包」这个自造名词、自动化收成两层并补编辑态、设置表单照 multica 重做、说明性文字压到 5.7k 字**（V3.29）；**文案见底——说明性文字压到 3.6k 并把实现约束归档进 `docs/implementation-notes.md`、记忆面换成结果语言、能力面删掉订阅 tab、偏好设置补个人资料、去掉形态限定**（V3.30）；**交付前逐项收口——运行时机器页分 adapter / 健康两个 tab、代码目录授权并进代码仓并支持本地目录与 git 自动绑定、插件补配置弹窗、通知重做、提交身份移到代码仓**（V3.31）；**运行时重组——右侧从「表 + 抽屉」改为「概览 + 每个 adapter 一个 tab」、机器左栏按本机/远程分组、项目设置改用设置面表单形态、代码仓合并成一个添加入口**（V3.32）；**执行机器不再分本地与远程、应用健康移出运行时归服务端、概览加 adapter 列表、执行组合表去状态加限额、adapter tab 改为概览 + 运行中 + 工具**（V3.33）；**「配置」正名为接入方式并只在一个 CLI 真有多套凭据时露出、adapter 状态只有在线离线、概览改卡片、插件重名直接拒绝、提交身份改读执行机器上的 git 配置**（V3.34）；**工具改成表格并补上 CLI 内置工具、adapter tab 去掉限宽、概览卡片一行铺开**（V3.35）；**运行时的「正在运行」改为只读，停止任务只在监控与任务本身**（V3.36）；**自动化的执行内容与生成的任务合成一块 Markdown「任务内容」并排到最后、记忆新增「策略」tab、三个面的页首改为标题在上解释在下**（V3.37）；**自动化概览收成「卡片行 + 触发条件 + 任务内容」、权限降为只读、启用改成开关、记忆的「关系」改名知识图谱、检索基础设施从健康度移到策略**（V3.38）；**自动化的只读事实收成标题下一行 meta、概览改名属性、时间表照 multica 做成结构化选择器不写 cron、执行组合可编辑**（V3.39）；**新增「每次触发产出什么」三档（每次都建 / 只在有发现时建 / 仅运行），推翻 V3.20 的「不提供仅运行模式」**（V3.40，V3.41 撤回）；**属性页改成「触发条件 ｜ 执行组合」并排 + 任务内容全宽，只读事实改两列一行一条**（V3.41）；**执行组合拆成模型与思考深度两个下拉、模型不支持的深度置灰、编辑态收回一屏**（V3.42）；**记忆的「健康度」改名「待处理」只留待办、读数移到策略并新增「试一次召回」、知识库补最近召回与采纳两列、知识库与知识图谱互通**（V3.43）；**记忆最终按内容 / 策略 / 故障 / 价值归位：待办合并、策略进入设置、系统诊断独立、效用进入统计，并清除重复配置**（V3.44） | 30 | **当前设计基线** |
制作过程目录不作为产品版本保留。版本目录必须能够**独立打开 `index.html`** 并运行自身交互回归（`browser-check.mjs`）。

> **不再导出截图。** 设计稿是可交互的，看 `index.html` 就够了；截图既会立刻过期，又要靠一份导出脚本跟着页面结构一起维护——它维护的是一份比原件更差的副本。冻结版本目录里的 `shots/` 是历史留存，不再更新。
