---
topics: [product-reset, user-journey, ux-prototype, self-test, replanning]
doc_kind: plan
created: 2026-08-12
updated: 2026-08-13
---

# PersonaHub 产品体验重置与开发计划调整方案

> 结论：暂停当前功能开发。先从本机 `clowder-ai` 与 `multica` 的真实代码还原各自用户
> 旅程，并分别还原可点击的参考页面；用户看过并选择借鉴方向后，再设计 PersonaHub
> 用户旅程、产品原型和开发自测试体系。F009–F012 保留为历史规划输入，但不得原样进入
> 开发，也不在本阶段修改业务代码、API、schema 或测试。
>
> **2026-08-12 方法修正**：参考页面的还原方式由「手写单文件静态 HTML」改为「渲染冻结」
> （第 3.1 节）。前者已实测失败两次，产物 `multica/prototype/` 与 `clowder-ai/prototypes/`
> 均为 agent 自造 design system 的结果，与真值无对应关系。后者已完成 multica 22 页、
> clowder-ai 5 页，验收全绿。

## 1. 背景与问题判断

v0.2 已完成大量功能、契约和自动化测试，但首次短时间 dogfood 就暴露出阻塞性问题和
核心操作不可发现的问题。根因不是单个组件质量不足，而是当前流程的完成定义主要围绕
Feature、FR/AC 和代码正确性，没有把“用户能否从目标出发完成一条完整旅程”作为发布
前置条件。

当前流程存在四个结构性缺口：

1. 用户旅程虽然散落在 PRD 和 Feature spec 中，但没有产品级、跨 Feature 的体验真相源。
2. user-facing Feature 没有强制原型门禁，0.2 可以在未做真实浏览器人工点击验证时收口。
3. `npm run verify` 不包含真实浏览器 `test:e2e`，组件测试无法发现可发现性和完整操作路径问题。
4. dogfood 发生在版本收口后，承担了本应由开发自测试和发布验收提前发现的问题。

## 2. 本轮目标、范围与暂停规则

### 目标

- 重新确认 PersonaHub 当前阶段最重要的用户目标和完整操作旅程。
- 用独立、可点击的 HTML 原型表达目标体验，不受现有 React 页面结构限制。
- 建立任务级（单元/接口）、需求级（Playwright 真实浏览器）、发布级（用户使用反馈）三层自测试流程。
- 评估现有 v0.3 计划受影响的范围，形成后续整体整改的输入。

### 本轮范围

- PRD 中 v0.1–v0.3 承诺范围内的用户目标：首次配置、任务执行、失败恢复。
- 从产品意图重新推导的信息架构、入口、状态反馈和错误恢复方式。
- 开发流程、Feature 门禁、E2E 和 dogfood 验收方式。

### 参考优先、PersonaHub 现状隔离原则

第一步不自行设计 PersonaHub，而是从本机 `D:\Projects\clowder-ai` 与
`D:\Projects\multica` 的 README、产品文档和真实前端代码分别还原用户旅程与页面状态，
再各做一个忠于原项目的静态 HTML 参考页。用户看过两套参考后，明确选择“直接借鉴 / 调整
借鉴 / 不采用”的方向，PersonaHub 用户旅程设计才能开始。

这里允许阅读两个参考项目的实现，但仍禁止用 PersonaHub 当前 `web/` 实现作为设计起点。
原因：参考项目是外部设计证据；PersonaHub 现有实现则是本轮 dogfood 判定需要重新检验的对象。

具体约束：

- 参考还原与 PersonaHub 旅程阶段不阅读 PersonaHub `web/` 源码，不以现有页面截图为设计输入。
- 两个参考项目必须分开还原，不先混成一套“理想设计”；旅程结论需引用具体代码/文档路径。
- 参考静态 HTML 用于展示原项目的页面结构、状态和点击路径，不添加 PersonaHub 特有功能。
- 不因"现在已经这样实现了"而保留某个入口、术语或步骤；只因"PRD 要求"而保留。
- 现有实现与既有 dogfood 问题记录（`dogfooding-bugs.md` / `dogfooding-notes.md`）
  **只在 P3 影响面分析阶段作为差距对照输入使用**，不进入旅程设计和原型设计阶段。

### 暂停规则

- F009–F012 保持 `draft`，不得推进到 `ready-for-development`。
- 暂停新业务代码、schema、API、组件重构和非紧急 bug 修复。
- 本阶段只允许调研、旅程文档、HTML 原型、检视记录和测试流程设计。
- 用户明确批准原型前，不输出可直接执行的详细代码任务，不提前实现“看起来确定”的页面。

## 3. 工作流 A0：参考项目用户旅程与页面还原

**预计时间：1–2 个工作日。必须先于 PersonaHub 用户旅程。**

1. 分别读取两个项目自身的 `AGENTS.md` / `CLAUDE.md`、README、产品文档、路由与关键页面代码。
2. 每个项目独立产出：目标用户、首次进入、创建核心工作、执行/协作、完成/恢复旅程，及页面/状态矩阵。
3. 每个项目独立还原代表性页面，**方法见第 3.1 节（不是手写静态 HTML）**。
4. 用真实浏览器验证还原页的点击连续性、键盘焦点和控制台错误（视口固定 1440px，理由见 RR006）。
5. 输出中立对照表；由用户决定哪些直接借鉴、哪些调整借鉴、哪些不采用。

**本轮关注点（2026-08-13 明确）**：参考还原服务于「分析页面交互与功能分区」，不服务于
多分辨率适配。因此优先级是 —— 交互契约（每个可交互元素的触发条件与状态变化）> 功能分区
（导航层级、区域划分、信息密度）> 视觉细节 > 响应式行为（本轮不做）。

**退出条件**：用户明确完成参考评审并给出借鉴方向。未达到时，不进入 PersonaHub 用户旅程设计。

### 3.1 页面还原方法：渲染冻结，不是手写 HTML

**本节修正了原计划中「制作单文件静态 HTML」的做法。该做法已实测失败，不再采用。**

#### 失败证据

2026-08-12 当晚曾用 agent 对两个项目各重绘过一次，产物仍在（均未提交）：

| 产物 | 规模 | 结果 |
|---|---|---|
| `D:\Projects\multica\prototype\` | 36 个 HTML + 373 行 CSS | 不像 |
| `D:\Projects\clowder-ai\prototypes\` | 11 个 HTML + 430 行 CSS | 不像 |

两份的失败模式完全相同 —— agent 自造了一套 design system：

| 项目 | 真实体系 | 重绘产物 |
|---|---|---|
| multica | Tailwind v4 `@theme inline` + OKLCH | 手写 `--brand: #3b6df2` 平铺变量 |
| clowder-ai | OKLCH 四旋钮派生（`--accent-hue` / `--surface-chroma`） | 手写 hex `--brand-pink: #f9af00` + emoji 图标 |

**根因**：agent 读 React 源码 → 脑内渲染 → 写 HTML，是「翻译」不是「提取」。Tailwind 类名的实际数值（间距、字阶、色值、阴影）由编译期算出，源码里根本不存在这些数字，agent 只能猜。猜 5000 行，必然处处偏差。

这也解释了为什么「再改细一点」没用：产物与真值之间没有对应关系，改一处偏一处。

#### 替代方法

让浏览器当唯一真相源，机器提取渲染结果。渲染后的 DOM + 编译后的 CSS 拼起来**就是原页面本身**。

四个阶段：真值提取 → 静态化 → 交互还原 → 自动验收。完整方法、脚本与踩坑清单见 `frontend-replication` skill（用户级，`~/.claude/skills/frontend-replication/`），本文档只记录两个参考项目的具体接入参数（第 3.2、3.3 节）。

**关键点**：静态化阶段只做减法（剥框架运行时、改资源引用），**不重写 CSS、不重命名 class、不重建 token**。打开发现不一样，是漏了某个 CSS 或字体，去补，不要动样式。

#### 已完成的实测结果（2026-08-12）

| 项目 | 页面数 | 验收项 | 结果 |
|---|---|---|---|
| multica | 22 | 66（22 DOM + 44 样式） | **66/66 通过** |
| clowder-ai | 5 | 15（5 DOM + 10 样式） | **15/15 通过** |

产物落点 `D:\Projects\ui-reference\`（独立目录，不进任何现有仓库）。验收标准为「结构与交互一致」：DOM 树逐节点比对 + 每节点约 60 个 computed style 属性比对，非像素级截图比对。

multica 已还原页面：landing / login / about / issues / my-issues / projects / agents / autopilots / skills / runtimes / squads / inbox / chat / usage / billing / settings / issue-detail / project-detail / agent-detail / skill-detail / member-detail / agent-new。

clowder-ai 已还原页面：settings / memory / chat / dev-overflow / showcase-f11。

### 3.2 multica 接入参数

| 项 | 值 |
|---|---|
| 前端 | `http://localhost:3002`（Docker `multica-frontend-1`） |
| 后端 | `http://localhost:3001`（Docker `multica-backend-1`） |
| 数据库 | 容器 `multica-postgres-1`，**宿主访问不到** |
| 栈 | Next.js 16 + Tailwind v4 + Base UI |
| 主题 | `<html class="dark">`，216 个 CSS 变量 |

**数据库只能走容器**：compose 里映射的 5433 被本机另一个 postgres（`lobe-postgres`）占用，所以宿主连不上。所有 SQL 走 `docker exec multica-postgres-1 psql -U multica -d multica -c '...'`。

**登录**：后端设了 `MULTICA_DEV_VERIFICATION_CODE=888888` 且 `APP_ENV=development`，任何邮箱都能用这个码登进去。流程是两个 POST：先 `/auth/send-code`（必须调，服务端要有验证码记录才肯验），再 `/auth/verify-code`。本机 SMTP 未接通，不会真发邮件。

**账号选择（重要）**：用 `qiaozhi_li@126.com`，不要新建账号。它已完成引导且填过来源问卷，天然没有弹窗；名下 `test` 工作区带产品内置的 4 个默认 agent（Code Developer / Code Reviewer / Plan Builder / Multica Helper）+ 3 个自建 agent + 4 个 skill + 1 个 project。

新建账号踩过的两个坑（现已绕过，仅作记录）：

1. 未完成引导 → 应用内路由重定向到 onboarding，节点数 144（正常 419）。需 `UPDATE "user" SET onboarded_at = now()`。
2. 首次进入弹「你是从哪里了解到 Multica 的？」来源调研，遮罩盖住整页、背景模糊，419 个节点里 112 个属于弹窗。触发条件在 `packages/views/onboarding/source-backfill-merge.ts`：`source` 有值或 `source_skipped: true` 即不弹。**不要用选择器删弹窗**，遮罩和 body 的 `overflow:hidden` 会残留。

**工作区**：`test`（slug）。详情页 ID —— issue 用人类可读标识 `TES-1`，其余用 UUID，从库里取。

**数据密度对比**：新建空工作区的 issues 页 307 节点、显示「还没有 issue」；`test` 工作区同一路由 565 节点，分组/状态色/优先级图标齐全。

### 3.3 clowder-ai 接入参数

| 项 | 值 |
|---|---|
| 前端 | `http://localhost:3003` |
| 后端 | `http://localhost:3004` |
| Redis | `6399`（自带端口隔离，不冲突） |
| 栈 | Next.js 14 + Tailwind v3 + 手写组件（无 Radix / 无 Base UI） |
| 主题 | `<html data-theme="dark">`，**667 个 CSS 变量**（每只猫一套 persona 色） |

**关键认知：Electron 只是套壳。** `desktop-dist/resources/app/main.js:67` 写死 `FRONTEND_PORT = 3003`，`:132` 是 `mainWindow.loadURL("http://localhost:3003")`。所以直接用浏览器访问 3003 即可，**不需要 CDP 控制 Electron**——渲染出的 DOM 完全一致，而走 CDP 反而要处理应用内导航（不能直接 goto 路由）。

**启动**：打开桌面应用即可，它会自己拉起前后端。若 3003 无监听说明服务没起来（界面可能还在但那是内存里的残留页面），重启应用即可。

**不要在仓库内落脚本或产物**：clowder-ai 是 cat-cafe 的下游同步镜像，git log 里有大量 `sync: cat-cafe <sha> → clowder-ai` 自动提交，在其中改动会与上游同步冲突。

**实时推送不是问题**：仓库里 55 个文件用 socket.io、34 个用 `setInterval`，原以为会让 DOM 永不安定。实测 chat 主页抓取 10.7s，与静态页相当——持续推送改的多是文本内容，DOM 结构很快就稳。无需断开 socket。

**建议排除 `pixel-brawl`**：phaser 游戏，内容全部画在 canvas 里，DOM 复刻出来是一块空白画布。`starry` / `mission-control` 若也是 canvas 主导，同样处理。

### 3.4 两个项目共同的坑（已固化进 skill，此处仅备查）

| 现象 | 根因 | 处理 |
|---|---|---|
| 整页报结构不同 | `<next-route-announcer>` 宽 0 不可见，但插入位置在两次加载间会变，导致其后所有兄弟节点索引偏移 | 加进 `removeSelectors` |
| 某个 input 颜色每次都不同 | 采样到 `transition-colors` 过渡中间值 | 提取与验证两端都注入冻结样式 |
| spinner 每次角度不同 | `animation-play-state: paused` 停在当前帧 | 改用 `animation: none` |
| 节点数在两次运行间跳动 | 挂载后才 fetch 的内容（如 star 数）赶不上静默窗口，二次运行走缓存就赶上了 | 预热重载，让每次抓取都是热加载 |
| 某控件多一圈焦点环 | 页面 `autofocus` | 提取前 blur + 静态化剥离 `autofocus` + 验证端同样 blur |
| 上百个宽度差异 | 字体没本地化，`@font-face` 指向源站，停机后 404 回退系统字体 | 下载字体到本地并改写 CSS 引用 |

**通用原则：提取端做的任何稳定化，验证端必须原样做一遍。** 实测两次栽在这条上（冻结动画、blur 都曾只做了一端），表现都是「某个 input 的颜色对不上」。

**唯一验收发现不了的错误**：公开页（落地页/登录页/营销页）在有 session 时会重定向进应用，抓到的是错页面，而 DOM diff 和样式 diff 都会报 ok —— 因为真值和复刻抓的是同一个错页面。必须人工给这些页面标 `anonymous: true`。实测 multica 的 `/` 曾抓成 419 节点的工作区页，而非 985 节点的落地页。

## 4. 工作流 A：PersonaHub 用户旅程梳理

**预计时间：1–2 个工作日。**

### A1. 产品意图重建

不做现状还原，直接从设计文档重新推导（见"清白起点原则"）：

- 读 `docs/personahub-prd.md`，抽出产品承诺的用户目标、核心概念和成功定义。
- 读 `docs/personahub-architecture.md` / `-system-design.md` / `docs/decisions/`，
  只提取**真实的技术约束**（本地优先、CLI adapter 进程模型、SQLite 单机、
  workspace 排他锁等），用来判断旅程是否可实现，不用来推导页面结构。
- 列出 PRD 层面的概念清单（Project / Issue / Thread / Agent / Workflow / Evidence…），
  标注每个概念是"用户必须理解的"还是"实现细节，不应出现在界面上"。

**产出**：产品意图摘要、技术约束清单、概念清单及其暴露级别。

### A2. 核心用户与核心任务确认

第一阶段只围绕一个核心用户：希望把真实 coding 工作交给多个本地 AI CLI，同时保留
控制权和验证证据的个人开发者。

优先验证三个 P0 任务：

1. 首次打开应用后完成最小配置并进入可执行状态。
2. 输入开发目标、确认系统建议并运行到可信的 Done。
3. 任务失败、中断或等待人工判断后，理解原因并安全恢复。

**产出**：用户定义、Jobs-to-be-Done、P0/P1 范围边界。

### A3. 目标旅程设计

每条旅程必须包含：

- 触发条件、用户目标与成功终点。
- 用户动作、系统反馈和此刻最关心的问题。
- 主路径、空状态、错误路径、恢复路径和不可逆操作。
- 每一步涉及的产品概念，以及哪些概念应隐藏或渐进展示。
- 可观察、可计时的验收标准。

**产出**：目标旅程表、状态矩阵、术语与信息披露规则。

### A4. 建议验收指标

- 首次用户无外部文档辅助，在 5 分钟内进入“可以创建任务”状态。
- 创建任务只有一个主入口，从输入目标到启动不超过 3 次主要操作。
- 运行页面优先回答“进行到哪一步、是否需要我、下一步是什么”。
- 阻塞状态必须同时展示原因、影响和一个推荐主操作。
- 用户无需阅读内部 error code 或原始日志完成常见恢复。

### A5. 旅程定稿门禁

- 第 1 轮：全量检查目标、范围、步骤、异常和恢复是否完整。
- 修改后第 2 轮：只复核 diff 及相邻步骤。
- Critical/High 体验问题清零，并由用户明确批准。

**退出条件**：用户旅程成为后续原型的唯一行为输入；未通过的争议点不得交给原型阶段猜测。

## 5. 工作流 B：PersonaHub 前端 HTML 原型

**预计时间：2–4 个工作日，在工作流 A 初稿通过后开始，与工作流 C 并行推进。**

### B0. UX 设计参考基线

原型的视觉与交互方向参考本机两个开源项目 `D:\Projects\clowder-ai` 与
`D:\Projects\multica`。参考方式已有现成的调研结论，**先读调研文档再看代码，不重做竞品分析**：

| 参考源 | 读哪一节 | 用来回答 |
|---|---|---|
| `docs/research/multica-analysis-report.md` | §4 页面效果与信息架构、§7.2 UI 转译 | 主视觉基调与工作台骨架 |
| `docs/research/clowder-ai-analysis-report.md` | §4 页面效果与信息架构、§7.2 UI 转译 | 协作/角色/证据类信息如何组织 |

调研已给出的结论直接作为本轮起点，不再重新论证：

- **视觉基调取 multica**：简洁、冷静、功能导向的工程协作工具风格；不采用 clowder-ai 的
  情感化、强 IP 角色和多工作面叙事 UI，避免早期界面复杂化。
- **协作逻辑取 clowder-ai**：Task Thread、Agent Profile、Handoff、Review、Trace、Memory
  这套"agent 是有身份的长期成员"的组织方式，但用 multica 的克制视觉去表达。
- **两份调研给出的页面骨架高度一致**，可作为原型第一版布局的默认假设，再按工作流 A 的
  旅程校正：左侧 Projects / Tasks / Agents / Settings，中间主区 Board 或 Task Detail，
  右侧上下文 Assignee / Runtime / Trace / Decisions / Blockers，Run Log / Thread 放详情区。

约束与执行节奏：

- 参考的是**页面级 UX 组织方式**，不是功能清单。任何"因为 multica/clowder 有这个功能所以
  我们也做"的推论一律拒绝——功能范围只由 PRD 和工作流 A 的旅程决定。
- 上述三条结论在 B1/B2 阶段**直接采纳，不再重新论证**。调研报告是基于 README 写的，页面
  骨架这一层已经够用，B0 阶段不花时间通读两个项目的前端代码。
- **只有在 B3/B4 遇到具体交互细节说不清时才去读真实代码**（典型场景：运行中状态怎么呈现进度
  与"是否需要我"、blocked 时怎么同时给出原因和推荐动作、日志/证据的折叠层级怎么分）。此时
  按 `docs/SOP.md`「参考开源项目验证设计假设的节奏」定点去挖，带着具体问题读，不做通读。

### B1. 信息架构草图

先确定页面关系，不先设计视觉细节：

- Project、任务、Agent、配置和运行状态的一级/二级层级。
- 创建任务的唯一主入口及高级选项的渐进展示方式。
- 执行进度、Thread、技术日志、人工介入和最终证据的关系。
- 桌面宽屏与 1024px 窗口下的信息优先级。

**产出**：页面地图和区域职责说明。

### B2. 低保真可点击骨架

首批覆盖以下状态：

1. 首次启动和渐进式设置。
2. 工作台空状态与创建任务。
3. 推荐方案确认。
4. 任务运行中与不需要人工介入。
5. Done 结果、Blocked 状态及恢复。

**产出**：单文件 HTML/CSS/JS 原型，可直接本地打开。

### B3. 交互和视觉细化

- 所有可点击目标有明确视觉提示、hover/focus/disabled 状态。
- 主操作每个视图最多一个；次要操作不与主操作竞争。
- 普通用户信息默认展开，原始事件、日志、ID 和规则命中详情默认折叠。
- Loading、empty、error、blocked、done 均有明确反馈和下一步。
- 沿用 `docs/decisions/0004-ui-styling-stack.md` 的样式栈与 OKLCH token 体系（这是已生效的
  技术决策，不是"现有实现"），但布局、信息密度和组件组合允许完全重新设计。

### B4. 原型检视

第 1 轮全量检视有限检查清单：

- 可发现性、信息层级、术语、操作反馈。
- 三条 P0 旅程是否可连续完成。
- 空状态、错误状态和恢复路径。
- 1024×768、1440×900、1920×1080 三档窗口。
- 键盘焦点、对比度和文本溢出等基础可访问性。

修改后第 2 轮只复核修订区域和相邻交互；仅在第 2 轮引入新问题时追加一次第 3 轮，
不进行无限全量重审。

### B5. 用户定稿

用户按场景清单亲自操作原型。反馈统一归为：

- 必须修改：阻塞旅程或明显违背预期。
- 后移：不影响 P0 旅程，可进入后续 Feature。
- 不采纳：记录理由，避免下一轮反复提出。

**退出条件**：用户明确说“原型定稿”；单纯没有新反馈不等于批准。

## 6. 工作流 C：开发自测试体系

**流程设计预计 1–2 个工作日，在工作流 A 初稿通过后开始，与工作流 B 并行推进；实际门禁在重构实施阶段落地。**

### C1. 三层测试模型

| 层级 | 责任 | 手段 | 执行时机 | 主要证据 |
|---|---|---|---|---|
| 任务级 | 一个实现动作没有破坏局部契约 | 单元测试 + 接口（API/集成）测试 + typecheck | 每个任务完成后 | 测试报告、`npm run verify` 通过记录 |
| 需求级 | 受影响的用户旅程切片能被真实用户走通 | **Playwright 真实浏览器端到端** | Feature 进入 review 前 | E2E 用例、失败截图/trace、异常与恢复用例 |
| 发布级 | 真实用户在真实环境能完成闭环并认可 | 人工 dogfood + 用户使用反馈 | 版本收口前 | scripted dogfood 记录、真实 CLI 运行证据、`dogfooding-bugs.md` / `dogfooding-notes.md` 回填 |

各层的职责边界，避免上一层替下一层背锅：

- **任务级只证明契约没坏，不证明好用**。jsdom 组件测试归在这一层，它验证组件行为，
  不能作为"页面能用"的证据。
- **需求级是本轮新增的强制层，也是这次问题的直接补丁**。仓库已有 Playwright 基建
  （`e2e/playwright.config.ts`、`npm run test:e2e`），但目前只有一条 `f005-layout.spec.ts`，
  且 `test:e2e` 未接入 `npm run verify`——能力早就在，缺的是强制执行。整改的核心动作是
  **补旅程覆盖 + 接入强制门禁**，不是重建测试框架。
- **发布级承认自己不可自动化**。它的价值就在于人的主观判断（"这是不是我预想的样子"），
  因此不设"自动化替代方案"，只固定场景清单和记录格式，保证每次收口都真的跑过。

### C2. 旅程—测试映射

用户旅程每一步必须映射到：

- 自动化测试文件或明确说明为何只能人工验证。
- 测试前置数据和环境。
- 用户动作与预期可见反馈。
- 失败恢复动作及其预期状态。
- 证据保存位置。

没有映射的旅程步骤不得靠"整体看起来测试很多"视为已覆盖。

**需求级 E2E 的第一批用例就是三条 P0 旅程本身**（A2 定义的：最小配置进入可执行状态、
输入目标运行到可信 Done、失败后理解原因并安全恢复），一条旅程一个 spec 文件，按用户动作
顺序断言，不按页面或组件拆分用例。这样做的原因：

- C2 的旅程—测试映射天然成立，不需要事后再补一张对照表。
- 直接避免这次 dogfood 暴露的问题模式——"组件渲染全部正确，但旅程走不通"。按组件拆的用例
  永远发现不了可发现性问题和步骤断裂。
- 组件级、边界值的细粒度断言留在任务级（jsdom / 接口测试），不挤进 E2E，避免 E2E 变慢变脆。

### C3. 统一验证入口整改要求

- `npm run verify` 保持本地高频入口，覆盖 lint/typecheck/单元/组件测试，不在这一层强制跑真实浏览器
  E2E，避免拖慢日常本地循环。
- 新增覆盖真实浏览器 E2E 的强制门禁命令（如 `npm run verify:release`），PR 合并前与版本收口前
  必须跑；本地与 CI 复用同一条命令，不允许 CI 另开一套 E2E 清单。
- jsdom UI 测试用于组件行为，不得替代真实布局和完整点击路径；真实布局验证只能靠 E2E 门禁完成。
- 真实环境测试未执行时必须明确失败或记录客观阻塞原因，不能写成通过。

### C4. 固定 dogfood 场景

版本收口前至少执行：

1. 干净数据库的首次启动和配置。
2. 已有 Project、历史 Issue 和既有 Adapter 的升级场景。
3. 一个真实 coding Issue 正常运行到 Done。
4. 主动取消、进程重启或 Adapter 不可用后的恢复。
5. validation 失败/中断、unblock、重试并最终完成。

### C5. 用户反馈的记录与回流

发布级的产出是主观判断，必须有固定落点，否则会像这次一样只留在记忆里：

- 使用中发现的确认缺陷 → `docs/reviews/dogfooding-bugs.md`，`npm run bug:log` 校验。
- 不算 bug 的交互/易用性/术语问题 → `docs/reviews/dogfooding-notes.md`。
- 每条记录必须能追溯到具体旅程步骤；反复出现在同一步骤的问题，说明该步骤的需求级 E2E
  覆盖不足，需要补用例而不是只修单点。
- 每次 dogfood 后回填 note 的状态与去向（`adopted` / `dismissed`），不允许长期堆积 `open`。

### C6. 完成与停止条件

- Critical/High 正确性和体验问题清零。
- 原型关键交互与实现一致。
- 自动化门禁全绿，真实浏览器旅程通过。
- 发布级 dogfood 三类数据状态通过。
- 任一项失败，版本回到整改；不得把核心路径不可用写入“已知限制”后继续收口。

## 7. 当前 v0.3 开发计划修改评估

### 总体判断

F009–F012 都处于 `draft`，当前暂停不会造成已实现代码或 migration 的返工，成本主要是
重新审视产品优先级和 Feature 边界。原计划技术依赖顺序基本自洽，但它从 Artifact、
Room、Squad 等领域能力出发，没有先证明当前核心工作台是否好用。因此建议保留技术研究，
废止现有时间线和“评审通过即可开发”的假设。

| 项目 | 当前判断 | 建议处理 | 原因 | 重新进入计划的条件 |
|---|---|---|---|---|
| F009 Artifact Foundation | 技术基础有价值，但不是当前体验阻塞的直接解法 | 保留 draft，重新评估 UI 范围 | 可能增加 Inspector 复杂度 | 原型明确 Artifact 在旅程中的用户价值与入口 |
| F010 Artifact Coding Slice | 依赖 F009，且会深度改变执行与结果呈现 | 暂停并重写旅程映射 | 当前核心执行旅程尚未稳定 | 新工作台整改完成，Artifact 消费路径通过原型验证 |
| F011 Work Room | 与“观察、打断、纠偏”高度相关，产品价值可能保留 | 保留概念，重做交互与范围 | 现有三栏 Thread 体验未验证，直接加 Room 风险最高 | 原型先证明单 Issue 的观察与介入模型 |
| F012 Squad | 便利性能力，不解决当前阻塞主流程 | 明确后移 | 依赖 F011，且会增加设置复杂度 | Room 被真实使用并出现重复成员配置需求 |
| v0.3 25–40 日时间线 | 建立在现有 Feature 顺序和范围不变的假设上 | 作废，定稿后重估 | 旅程和整体整改会改变范围 | 整改影响面、迁移方案和测试矩阵完成 |

### 建议替换后的阶段顺序

P2（工作流 B）与 P3（工作流 C）共享同一启动点（工作流 A 初稿通过），并行执行，不是串行。
P0–P3 合计预留 **2 周（10 个工作日）**硬性时间盒；到期若仍未定稿，重新评估范围而非无限顺延。
P4 是原型定稿后才启动的独立实施阶段，不计入这两周窗口。

| 阶段 | 内容 | 退出条件 | 粗估 |
|---|---|---|---:|
| P0 开发冻结与基线 | 固定当前版本、保留问题证据、不扩功能 | 冻结边界明确 | 0.5 天 |
| P1 用户旅程 | 工作流 A | 用户批准旅程 | 1–2 天 |
| P2 HTML 原型（与 P3 并行） | 工作流 B | 用户批准原型 | 2–4 天 |
| P3 自测试与整改设计（与 P2 并行） | 工作流 C + 代码/数据影响面 | 重构方案和测试矩阵通过检视 | 1–2 天 |
| P4 整体整改 | 按旅程垂直切片实施 | 每条 P0 旅程真实通过 | 原型定稿后估算，不计入两周窗口 |

### 需要重新评估的现有文档

原型批准后再修改，当前只登记影响面：

1. `docs/personahub-prd.md`：信息架构、核心流程、UI 需求和版本路线是否需要调整。
2. `docs/features/0.3/README.md`：版本目标、Feature 顺序、里程碑和验收旅程。
3. F009–F012 三件套：用户场景、UX 需求、非目标、依赖和任务顺序。
4. `docs/SOP.md` / `docs/features/README.md`：加入强制旅程、原型和真实浏览器门禁。
5. `package.json` / CI：统一 verify、E2E 和发布级 dogfood 入口。

## 8. 整体整改阶段的规划原则

原型定稿后先做影响面分析，不立即全仓重写。**这里是现有实现第一次被允许作为输入的地方**
（见"清白起点原则"）：把定稿旅程与现有实现逐条对照，产出「可复用 / 需改造 / 需废弃」三类
结论，并把 `dogfooding-bugs.md` / `dogfooding-notes.md` 的开放项映射到对应旅程步骤，确认
新设计确实解掉了它们，而不是换个位置重现。

整改任务按用户旅程垂直切片，而不是按"先全部后端、再全部前端"分层：

1. 首次设置切片：Project、Workspace、Agent、诊断与相应测试。
2. 创建任务切片：统一入口、推荐确认、启动反馈与幂等测试。
3. 运行观察切片：进度、关键事件、日志层级和人工补充。
4. 阻塞恢复切片：原因、影响、推荐动作、恢复状态机和回归测试。
5. 完成结果切片：结果摘要、文件变化、验证证据和可信 Done。

每个切片独立实现、独立验证、独立复核；上一个切片未通过真实浏览器旅程前，不进入
下一个切片。

## 9. 风险与控制

| 风险 | 影响 | 控制方式 |
|---|---|---|
| 原型不断扩展到 v0.3 新能力 | 无法定稿，重构范围失控 | 首轮只覆盖 PRD 中 v0.1–v0.2 承诺范围；新增想法进入后移清单 |
| clean-room 变成"重新发明一遍" | 白白丢掉已被验证有效的设计判断 | 允许并鼓励复用 PRD/ADR/调研结论；被禁止的只是把现有页面结构和代码逻辑当成设计输入 |
| 参考项目功能被顺手抄进范围 | 范围膨胀，偏离 PersonaHub 自身定位 | B0 已限定只参考页面级 UX 组织方式；功能范围只由 PRD 与工作流 A 决定 |
| 只调整视觉、不改变旅程 | 阻塞问题换皮保留 | 每个页面决策必须追溯到旅程步骤 |
| 原型与真实约束脱节 | 定稿后实现不可行 | 关键状态/API 假设同步做轻量可行性核对，不提前编码 |
| 全仓一次性重构 | 回归范围过大，无法定位问题 | 按五个垂直切片逐个闭环 |
| 自动化再次替代人工使用 | 可发现性问题重复发生 | 发布前固定真实浏览器 dogfood，不允许豁免核心旅程 |

## 10. 评审、批准与执行门禁

本计划采用有限两轮文档检视：

1. 第 1 轮全量检查范围、依赖、产出、退出条件和现有计划影响。
2. 根据反馈修改。
3. 第 2 轮只复核修改 diff 及相邻契约。
4. 用户明确批准后，本计划才成为执行依据。
5. 批准前不继续用户旅程、HTML 原型、自测试整改或业务开发。

## 11. 计划阶段最终交付物

- 一份经批准的产品级用户旅程文档。
- 一个经批准的可点击 HTML 原型及场景清单。
- 一份旅程—自动化—dogfood 测试矩阵。
- 一份整体整改影响面与垂直切片实施计划。
- 一份重新评估后的 v0.3 版本计划，明确 F009–F012 的保留、重写或后移结论。

## 12. 执行任务清单

本节是本计划的**执行进度真相源**。开始一项时在任务末尾标记 `（进行中）`；完成并验证后
立即把 `[ ]` 改为 `[x]`，不得攒到最后统一补勾。`[P]` 只用于修改不同文件且无顺序依赖的任务。
P2 与 P3 两个阶段整体并行，但各阶段内部按顺序执行。进度按已勾选数量计算，不另维护百分比。

### 产出物落点（先定路径，避免边做边挪）

| 产出 | 路径 | 性质 |
|---|---|---|
| 用户旅程文档 | `docs/personahub-user-journeys.md` | 产品级真相源，需登记进 `docs/README.md` 所有权矩阵 |
| 可点击 HTML 原型 | `docs/prototype/index.html` | 单文件，可直接本地打开 |
| clowder-ai 旅程还原 | `docs/research/clowder-ai-user-journeys.md` | 参考证据，不是 PersonaHub 真相源 |
| multica 旅程还原 | `docs/research/multica-user-journeys.md` | 参考证据，不是 PersonaHub 真相源 |
| clowder-ai 页面还原 | `D:\Projects\ui-reference\clowder\` | 渲染冻结产物，仓外独立目录，勿入库 |
| multica 页面还原 | `D:\Projects\ui-reference\multica\` | 渲染冻结产物，仓外独立目录，勿入库 |
| 参考借鉴决策表 | `docs/reviews/reference-borrowing-decisions.md` | 用户评审入口；记录直接借鉴/调整/不采用 |
| 旅程—测试映射矩阵 | `docs/reviews/journey-test-matrix.md` | 过程产物 |
| 影响面与切片实施计划 | `docs/reviews/refactor-impact-plan.md` | 过程产物 |
| 检视进行中记录 | `docs/reviews/CURRENT-doc.md` | 按 SOP 生命周期纪律，由检视人删除 |

### Phase P0：开发冻结与基线（0.5 天）

- [x] R001：在 `BACKLOG.md` 与 `docs/features/0.3/README.md` 标注 v0.3 暂停、原因与本计划链接；
  F009–F012 保持 `draft` 不变。
- [x] R002 [P]：记录冻结基线——当前 HEAD commit、最近一次全绿 CI run id、`npm run bug:log`
  输出快照，作为整改前后的对照点。
- [x] R003：按第 13 节规则逐份判定工作区已有草稿（旅程/原型/冻结规则）为保留输入、重写或撤销，
  并写下判定理由。

#### P0 执行记录（2026-08-12）

- 冻结 commit：`002568142e0a4e8611d23b0992c3f9037a81a49c`；执行前 `main` 与
  `origin/main` 一致且工作区干净。
- CI 对照点：无可记录的全绿 run id。通过 GitHub Actions API 查询
  `Qiaozhi94/personahub` 的 workflow runs，结果为空；仓库虽已有 `.github/workflows/ci.yml`，
  但截至冻结时尚无远端 run 记录。后续不得把“无运行记录”表述为 CI 通过。
- `npm run bug:log` 快照：共 3 条（2 `fixed`、1 `open`）；severity 为 2 高、1 中；
  唯一开放项为 `BUG-003`（高）“中断 validator 死锁 round 槽位，重验证无法开始”。
- 旧草稿判定：工作树、ignored 文件及全部 Git 历史中均不存在
  `docs/personahub-user-journeys.md` 或 `docs/prototype/index.html`，也未找到独立冻结规则草稿。
  因此三类旧草稿均按“无可验证来源，视为不存在/不可复用”处理；P1/P2 必须从本计划指定的
  真相源重新产出，不得以 `docs/features/0.1/ux-prototype.html` 等历史 Feature 原型替代。

### Phase P0.5：参考项目还原（1–2 天）

- [ ] RR001：分别读取 `D:\Projects\clowder-ai` 与 `D:\Projects\multica` 的项目约束、README、
  产品文档、路由和关键页面代码，建立“旅程步骤 → 代码/文档证据”索引。（进行中）
- [ ] RR002：产出 clowder-ai 用户旅程与页面/状态矩阵，不混入 PersonaHub 需求。
- [ ] RR003：产出 multica 用户旅程与页面/状态矩阵，不混入 PersonaHub 需求。
- [x] RR004：还原 clowder-ai 代表性页面。**方法变更**：不手写静态 HTML，改用第 3.1 节的
  渲染冻结流水线。已完成 5 页（settings / memory / chat / dev-overflow / showcase-f11），
  验收 15/15 通过。
- [x] RR005：还原 multica 代表性页面，同上。已完成 22 页，验收 66/66 通过。
- [ ] RR006：用真实浏览器验证还原页的连续点击路径、键盘焦点和控制台错误；记录结果与限制。
  结构与样式已由脚本自动验收，本项只需人工过一遍交互路径。
  **1024px 断点已取消（2026-08-13 用户决定）**：本轮参考还原的目的是分析页面交互与功能
  分区，不涉及多分辨率场景，因此只保留 1440px。若后续需要，改 `viewports` 配置增加一项
  即可重跑，快照按 `<页面>__<主题>__<视口>` 分目录并存，不影响已有产物。
- [ ] RR007：生成中立对照与借鉴决策表，等待用户逐项选择“直接借鉴 / 调整借鉴 / 不采用”。

### Phase P1：工作流 A PersonaHub 用户旅程（1–2 天，RR007 批准后）

- [ ] R004：A1 产品意图重建——从 PRD 抽出用户目标、核心概念与成功定义；从 architecture /
  system-design / ADR 抽出真实技术约束。**全程不读 `web/` 源码。**
- [ ] R005：A1 概念清单，逐个标注"用户必须理解"或"实现细节、不应出现在界面"。
- [ ] R006：A2 确认核心用户定义与三个 P0 任务，划定 P0/P1 范围边界。
- [ ] R007：A3 结合用户批准的参考借鉴决策设计三条目标旅程，每条含触发条件、用户动作、
  系统反馈、空/错误/恢复路径、
  概念暴露级别与可观察验收标准。
- [ ] R008：A4 把五条验收指标写进旅程文档，逐条给出可计时/可观察的判定方式。
- [ ] R009：A5 第 1 轮全量检视（目标、范围、步骤、异常、恢复完整性）并修复。
- [ ] R010：A5 第 2 轮只复核 diff 及相邻步骤；Critical/High 清零后由用户明确批准。
- [ ] R011：把旅程文档登记进 `docs/README.md` 所有权矩阵与导航，跑 `npm run check:doc-links`
  与 `check:doc-ownership` 通过。

### Phase P2：工作流 B HTML 原型（2–4 天，与 P3 并行）

- [ ] R012：B0 读两份调研的 §4、§7.2，落一页参考基线结论。**本阶段不读两个项目的前端代码。**
- [ ] R013：B1 信息架构草图——页面地图与区域职责，含 1024px 窗口下的信息优先级。
- [ ] R014：B2 低保真可点击骨架，覆盖首次设置、空状态与创建任务、推荐确认、运行中、
  Done/Blocked 恢复五个状态。
- [ ] R015：B3 交互与视觉细化——可点击目标的 hover/focus/disabled、每视图单一主操作、
  信息折叠层级、五种状态反馈；沿用 ADR 0004 的样式栈与 OKLCH token。
- [ ] R016：B4 第 1 轮全量原型检视，按有限清单逐条过（可发现性、三条旅程连续性、
  异常与恢复、三档窗口、基础可访问性）。
- [ ] R017：B4 第 2 轮只复核修订区域与相邻交互；仅在第 2 轮引入新问题时追加第 3 轮，封顶。
- [ ] R018：B5 用户按场景清单亲自操作原型，反馈归为必须修改 / 后移 / 不采纳并逐条记录。
- [ ] R019：用户明确说出"原型定稿"，写入文档并标注日期；没有新反馈不等于批准。

### Phase P3：工作流 C 自测试与整改设计（1–2 天，与 P2 并行）

- [ ] R020 [P]：C1/C2 产出旅程—测试映射矩阵，三条 P0 旅程每一步映射到测试文件或
  人工验证理由、前置数据、预期反馈、恢复动作与证据落点。
- [ ] R021 [P]：C3 定稿双入口方案——`npm run verify`（本地高频，不含 E2E）与
  `verify:release`（含 Playwright，PR 与收口前强制）的命令组成与 CI job 划分。
  **本阶段只出方案，不改 `package.json`。**
- [ ] R022 [P]：C4/C5 固定五个 dogfood 场景清单与记录模板，明确回填 `dogfooding-bugs.md` /
  `dogfooding-notes.md` 的时机与状态流转规则。
- [ ] R023：影响面分析——定稿旅程逐条对照现有实现，产出「可复用 / 需改造 / 需废弃」三类结论。
  **这是现有实现第一次被允许作为输入。**
- [ ] R024：把 `dogfooding-bugs.md` / `dogfooding-notes.md` 的开放项映射到对应旅程步骤，
  确认新设计确实解掉而非换位重现；同步回填两份主表的状态与去向。
- [ ] R025：产出五个垂直切片的实施计划与各自退出条件、迁移方案与回归范围。
- [ ] R026：P3 检视——测试矩阵与重构方案过一轮检视，Critical/High 清零。

### 时间盒检查点

- [ ] R027：第 5 个工作日做一次中检——P1 是否已批准、P2/P3 是否按期推进。未达进度时
  **缩小范围**（例如原型只覆盖三条 P0 旅程的主路径），不顺延两周窗口。
- [ ] R028：第 10 个工作日收口 P0–P3，输出第 11 节的五份交付物；仍未定稿的部分明确记录
  卡在哪一步、缺什么输入，不含糊写成"基本完成"。

### Phase P4：整体整改

原型定稿前**不展开**任务。R025 的切片实施计划批准后，另建任务清单或按 SOP 拆成 Feature
三件套执行；本节不提前枚举，避免在旅程未定稿时产生虚假的可执行任务。

## 13. 当前工作区说明

在本计划提出前，工作区已存在未提交的用户旅程、HTML 原型和冻结规则草稿。它们均为
探索性内容，不代表用户批准，也不作为本计划已执行的证据。本计划获批后，应先决定这些
草稿是保留为输入、重写还是撤销，再开始 P1；不得因文件已经存在而跳过旅程或原型评审。

判断标准直接套用"清白起点原则"：这些草稿如果是照着现有页面/代码整理出来的，一律作废重做；
只有确实从 PRD 出发推导的部分才允许作为 P1 的输入。无法判断来源的，按作废处理。
